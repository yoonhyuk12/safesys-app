# 프로젝트 삭제 연관 건수 표시 컨텍스트 노트

- 집계 기준은 프로젝트 삭제 시 함께 삭제되는 데이터 중 사용자가 업무 화면에서 입력한 최상위 기록이다.
- `projects` 기본행은 사용자 요청에 따라 제외한다.
- `project_shares`, `worker_registration_tokens` 같은 권한·등록용 기술 데이터와 상세 하위행은 간략 요약 취지에 따라 제외한다.
- 모달 요약은 안전·점검, 근로자, 작업·TBM, 품질·자재·계약의 네 범주로 표시한다.
- 건수 조회 권한은 실제 삭제 API와 동일하게 발주청 역할 또는 프로젝트 소유자로 제한한다.
- 조회가 끝나기 전 또는 조회 실패 상태에서는 사용자가 연관 건수를 확인하지 못했으므로 삭제 확정을 막는다.
- 집계 API는 지정된 22개 최상위 입력 테이블을 `HEAD`와 `count: exact`로 병렬 조회하며, 한 테이블이라도 실패하면 테이블명을 서버 로그에 남기고 전체 조회를 실패 처리한다.
- 모달의 총합과 네 범주는 API 응답을 단일 기준으로 사용하며, 총합이 0건이면 별도의 빈 상태 문구를 표시한다.
- 빠른 프로젝트 전환이나 모달 종료 뒤에는 effect cleanup 플래그로 이전 요청의 응답을 무시한다.
- `npx tsc --noEmit`과 `npm run lint`는 모두 종료 코드 0으로 통과했다. 린트의 기존 저장소 경고 외에 이번 변경 파일에서 새 경고는 발생하지 않았다.

---

# 사업현황 품질시험 건수 확장 컨텍스트 노트

- 기존 `등록 건수`는 `quality_test_records`의 프로젝트별 행 수를 유지한다.
- `확인시험 건수`는 `quality_verification_requests`, `총괄표 건수`는 `quality_summary_reports`의 프로젝트별 행 수로 정의한다.
- 세 건수는 같은 활성 프로젝트·사용자 조직 범위를 적용하며 본부·지사 소계는 프로젝트별 건수의 합으로 계산한다.
- 기존 미커밋 변경이 있는 품질시험 상세 화면 파일은 수정하지 않는다.
- `npx tsc --noEmit`과 `npm run lint`는 종료 코드 0으로 통과했다. 린트에는 저장소 전반의 기존 경고만 남아 있다.
- 품질시험 대장 지사 표도 품질 월례보고와 동일하게 모바일에서만 첫 열을 고정하고 지사명을 세 글자로 줄인다.
- 표는 `w-max min-w-full`로 콘텐츠 너비와 화면 최소 너비를 함께 보장해 360px 화면에서 가로 스크롤되도록 한다.
- 품질시험 대장 모바일 개선과 월례보고 현재 월 강조 후 `npx tsc --noEmit`, `npm run lint`가 종료 코드 0으로 통과했다. React 체크리스트에서도 이번 변경으로 추가된 훅·접근성·키 문제는 없다.

---

# 사업현황 품질 월례보고 연간현황 컨텍스트 노트

- 기존 조회가 `quality_monthly_reports`의 모든 연도를 이미 읽으므로 추가 데이터베이스 요청 없이 프로젝트별 제출 연월 목록을 응답에 포함한다.
- 선택 연도는 화면 로컬 상태로 관리하며 최초값은 현재 연도다.
- 연간 표에서는 미래 월을 미제출로 오해하지 않도록 제출이 없는 셀을 빨간색이 아닌 `-`로 표시한다.
- `/business` 메인 카드의 이번 달 제출 요약은 기존 탐색 맥락을 유지하기 위해 변경하지 않는다.
- `npx tsc --noEmit`과 `npm run lint`는 종료 코드 0으로 통과했다. 린트에는 저장소 전반의 기존 경고만 남아 있고 이번 변경 파일의 새 경고는 없다.
- 모바일 지사 표는 첫 열만 `sticky`로 고정하고, 데스크톱에서는 기존 자동 너비와 일반 위치로 복귀시킨다.
- 모바일 지사명은 원문을 `title`에 유지하면서 화면에는 세 글자와 `...`를 표시한다.
- 표에 콘텐츠 기준 너비를 부여해 360px 화면에서도 스크롤 컨테이너가 실제 가로 오버플로를 갖도록 한다.
- 모바일 개선 후 `npx tsc --noEmit`과 `npm run lint`가 종료 코드 0으로 통과했다. 린트에는 기존 저장소 경고만 남아 있다.
- 지사별 월례보고 표의 현재 월 강조는 선택 연도가 현재 연도일 때만 적용해 과거·미래 연도의 같은 월을 오늘로 오인하지 않도록 한다.

---

# 품질 월례보고서 동일 공종 물량 동기화 컨텍스트 노트

- 피드백 위치는 월례보고서 행의 세 물량 입력칸이며, 요구 범위는 같은 공종으로 추가된 시험행의 연간·이번 달·다음 달 시공 물량 동기화다.
- 기존에는 콘크리트 슬럼프, 토공 일부 시험, 강관서포트·강관비계만 별도 규칙으로 숫자 입력을 전파한다.
- 실시대장 시험실적 연동으로 동일 공종의 새 시험항목 행이 자동 추가될 수 있으므로 특정 프리셋 이름이 아닌 동일 공종 기준으로 처리한다.
- 횟수·시험실적 열의 기존 공종별 규칙은 유지하고, 동일 공종 일반 전파는 첫 행에서 입력한 `yearlyPlan`, `monthVolume`, `nextMonthPlan`에 적용한다.
- 기존 미커밋 변경이 있는 `QualityTestRecordsTab.tsx`는 수정하지 않는다.
- 공종명 비교 시 앞뒤 공백을 제거하되 저장된 원문은 변경하지 않는다.
- `npx tsc --noEmit`과 `npm run lint`는 종료 코드 0으로 통과했다. 린트에는 저장소 전반의 기존 경고만 남아 있고 이번 변경 파일의 새 경고는 없다.

---

# 품질시험 실시대장 삭제 권한 확대 컨텍스트 노트

- 기존 실시대장 삭제 버튼과 클라이언트 삭제·재정렬 로직은 기록 `created_by`가 현재 사용자와 같은 경우에만 동작한다.
- 요구 권한은 프로젝트 `created_by`와 현재 사용자가 같거나 현재 사용자 역할이 `발주청`인 경우다.
- 서비스 역할을 쓰는 서버 라우트에서 Bearer 토큰을 검증하고 동일 권한을 다시 확인해 UI 우회 호출을 차단한다.
- 삭제 단위는 기존과 동일하게 선택 행의 일련번호 전체이며, 삭제 후 프로젝트 내 남은 일련번호를 1부터 연속으로 재정렬한다.
- `QualityTestRecordsTab.tsx`에 겹쳐 있던 프리셋·표 열 변경과 그 밖의 기존 미커밋 변경은 이번 권한 커밋에서 제외해 보존한다.
- 클라이언트의 직접 삭제·재정렬은 제거하고 서버가 반환한 재정렬 계획으로 편집 중인 일련번호만 보정한다.
- 삭제 가능 여부는 별도 상태로 복제하지 않고 현재 사용자 역할과 프로젝트 소유자에서 파생해 React 상태 동기화 문제를 피한다.
- `npx tsc --noEmit`, `npm run lint`, `git diff --check`가 종료 코드 0으로 통과했다. 린트에는 저장소 전반의 기존 경고만 남아 있다.

---

# 사고 이력 프로젝트 검색 선택 컨텍스트 노트

- 피드백 대상은 `AccidentEntryModal.tsx`의 프로젝트 선택 필드다.
- 표시 형식은 `프로젝트명 (본부 · 지사명)`으로 통일한다.
- 검색 대상은 프로젝트명, 본부명, 지사명이며 여러 검색어는 순서와 무관하게 모두 포함되는 프로젝트를 찾는다.
- 기존 프로젝트 정렬, `project_id` 저장 값, 사고 수정 시 프로젝트 변경 금지 동작은 유지한다.
- 별도 UI 의존성을 추가하지 않고 현재 모달 안에 키보드와 스크린 리더를 지원하는 콤보박스를 구현한다.
- 브라우저에서 `지석 강화` 검색 시 `지석지구 배수개선사업  토목공사 (경기 · 강화·옹진지사)` 1건만 표시되는 것을 확인했다.
- Enter 키 이벤트로 검색 결과가 선택되고 전체 라벨이 입력란에 반영되며 목록이 닫히는 것을 확인했다.
- 브라우저 콘솔에는 Fast Refresh 로그만 있었고 오류는 없었다.
- `npx tsc --noEmit`, `npm run lint`, `git diff --check`가 종료 코드 0으로 통과했다. 린트에는 저장소 전반의 기존 경고만 남아 있고 이번 변경 파일의 새 경고는 없다.

---

# 점검 지적사항 분류코드 저장 컨텍스트 노트

- 정기점검 분류 저장 위치는 개별 사진 지적행인 `safety_inspection_results.finding_category_code`다.
- 본부불시점검은 한 행에 지적 2개가 있으므로 `headquarters_inspections.issue1_category_code`, `issue2_category_code`를 사용한다.
- 사용자는 기타 91건 중 메타·상태 문구 29건 제외, 기존 코드 재분류 60건, CCTV 2건 F19 유지로 확정했다.
- 신규 `F20_WORK_METHOD`의 표시명은 `작업방법 미준수`이며 상하 동시작업 위험을 포함한다.
- DB 트리거가 신규 입력과 원문 수정 시 코드를 자동 계산하는 권위 있는 저장 경로다.
- 사고 분석은 저장 코드를 우선 사용하되 SQL 적용 전후 호환을 위해 원문 분류 fallback을 유지한다.
- Supabase MCP는 읽기 전용이므로 마이그레이션 SQL은 사용자가 Supabase 콘솔에서 적용한다.
- Orca 작업은 Claude 구현 터미널을 중단한 뒤 SQL·앱 구현과 보정을 Grok 터미널에 재배분했다. 최종 Grok 독립 재검수에서 SQL·TypeScript 37개 패리티 케이스가 0건 불일치로 통과했다.
- `F20_WORK_METHOD`는 상하 작업과 동시 진행이 함께 있는 문장 또는 명시적 작업방법 미준수에만 적용한다. 일반적인 `동시 진행` 문구만으로는 F20으로 분류하지 않는다.
- 앱 조회가 새 분류 컬럼을 직접 선택하므로 운영 순서는 Supabase SQL 적용과 검증, 앱 배포 순이다. SQL 미적용 상태에서 앱 코드만 배포하면 PostgREST 컬럼 조회 오류가 발생한다.
- `npx tsc --noEmit`, `npm run lint`, `git diff --check`, TypeScript 골든 분류 19건이 종료 코드 0으로 통과했다. 린트에는 저장소 전반의 기존 경고만 남아 있다.
- 사용자의 SQL 적용 후 Supabase 실데이터에서 분류 컬럼 3개, 자동 분류 트리거 2개, CHECK 제약조건 3개와 분류 함수 존재를 확인했다.
- SQL 분류 함수 골든 케이스 19건이 모두 통과했고, 저장 코드와 함수 재계산 결과가 다른 행, 분류 가능한데 NULL인 행, 메타 문구인데 코드가 저장된 행은 모두 0건이었다.
- 초기 검토 코퍼스 786개 중 기존 F19 91개는 사용자 확정 내용과 동일하게 제외 29개, 다른 코드 60개, CCTV F19 유지 2개로 백필됐다.
- 브라우저 기본 필터 `2026-01-01 ~ 2026-07-18`에서 지적사항 분류 순위가 총 549건(정기 271건, 본부불시 278건)으로 표시되고 F20 1건까지 집계되는 것을 확인했다.
- 사고 유형별 순위와 지적사항 분류 순위는 같은 y좌표의 2열 카드로 나란히 렌더링되며 브라우저 콘솔 오류와 프레임워크 오류 오버레이가 없었다.
- 경기 기본 필터의 F19 3건은 CCTV 2건과 `공사 준비중으로 해당사항 없음` 메타 1건이었다. 전국에는 같은 작업없음·준비·중지 메타가 총 10건 남아 있었다.
- 후속 `database/20260718-1200_exclude_non_finding_status_texts.sql`은 분류 함수를 보정하고 기존 저장 코드를 재백필한다. 적용 후 경기 기본 필터의 F19 기대값은 CCTV 2건이다.
- 최종 독립 재검수에서 기존·신규 73개 SQL/TypeScript 패리티 케이스가 불일치 0건으로 통과했고, 1120 원본과 1200 후속 SQL의 분류 함수 본문이 동일함을 확인했다.
- 사용자의 1200 후속 SQL 적용 후 경기 기본 필터 실측은 총 548건(정기 271건, 본부불시 277건), F19 2건, F20 1건이며 브라우저 표시와 Supabase SELECT 결과가 일치했다.
- 최종 F19 2건은 평택의 `이동형cctv 설치위치를 작업구간으로 조정 필요`와 `Cctv 공사현장을 비추도록 설치할 것`이다. 전국 저장 F19의 작업없음·준비·중지·해당사항 메타 잔류는 0건이다.
- 후속 적용 뒤 저장 코드와 함수 재계산 불일치, 분류 가능한데 NULL인 행, 메타인데 코드가 저장된 행은 모두 0건이며 트리거 2개와 CHECK 제약조건 3개도 보존됐다.
