# 프로젝트 삭제 연관 건수 표시 컨텍스트 노트

- 집계 기준은 프로젝트 삭제 시 함께 삭제되는 데이터 중 사용자가 업무 화면에서 입력한 최상위 기록이다.
- `projects` 기본행은 사용자 요청에 따라 제외한다.
- `project_shares`, `worker_registration_tokens` 같은 권한·등록용 기술 데이터와 상세 하위행은 간략 요약 취지에 따라 제외한다.
- 모달 요약은 안전·점검, 근로자, 작업·TBM, 품질·자재·계약의 네 범주로 표시한다.
- 건수 조회 권한은 실제 삭제 API와 동일하게 발주청 역할 또는 프로젝트 소유자로 제한한다.
- 조회가 끝나기 전 또는 조회 실패 상태에서는 사용자가 연관 건수를 확인하지 못했으므로 삭제 확정을 막는다.
- 집계 API는 지정된 22개 최상위 입력 테이블을 `HEAD`와 `count: exact`로 병렬 조회하며, 한 테이블이라도 실패하면 테이블명을 서버 로그에 남기고 전체 조회를 실패 처리한다.
- 모달의 총합과 네 범주는 API 응답을 단일 기준으로 사용하며, 총합이 0건이면 별도의 빈 상태 문구를 표시한다.
- 빠른 프로젝트 전환이나 모달 종료 뒤에는 effect cleanup 플래그로 이전 요청의 응답을 무시한다.
- `npx tsc --noEmit`과 `npm run lint`는 모두 종료 코드 0으로 통과했다. 린트의 기존 저장소 경고 외에 이번 변경 파일에서 새 경고는 발생하지 않았다.

---

# 사업현황 품질시험 건수 확장 컨텍스트 노트

- 기존 `등록 건수`는 `quality_test_records`의 프로젝트별 행 수를 유지한다.
- `확인시험 건수`는 `quality_verification_requests`, `총괄표 건수`는 `quality_summary_reports`의 프로젝트별 행 수로 정의한다.
- 세 건수는 같은 활성 프로젝트·사용자 조직 범위를 적용하며 본부·지사 소계는 프로젝트별 건수의 합으로 계산한다.
- 기존 미커밋 변경이 있는 품질시험 상세 화면 파일은 수정하지 않는다.
- `npx tsc --noEmit`과 `npm run lint`는 종료 코드 0으로 통과했다. 린트에는 저장소 전반의 기존 경고만 남아 있다.
- 품질시험 대장 지사 표도 품질 월례보고와 동일하게 모바일에서만 첫 열을 고정하고 지사명을 세 글자로 줄인다.
- 표는 `w-max min-w-full`로 콘텐츠 너비와 화면 최소 너비를 함께 보장해 360px 화면에서 가로 스크롤되도록 한다.
- 품질시험 대장 모바일 개선과 월례보고 현재 월 강조 후 `npx tsc --noEmit`, `npm run lint`가 종료 코드 0으로 통과했다. React 체크리스트에서도 이번 변경으로 추가된 훅·접근성·키 문제는 없다.

---

# 사업현황 품질 월례보고 연간현황 컨텍스트 노트

- 기존 조회가 `quality_monthly_reports`의 모든 연도를 이미 읽으므로 추가 데이터베이스 요청 없이 프로젝트별 제출 연월 목록을 응답에 포함한다.
- 선택 연도는 화면 로컬 상태로 관리하며 최초값은 현재 연도다.
- 연간 표에서는 미래 월을 미제출로 오해하지 않도록 제출이 없는 셀을 빨간색이 아닌 `-`로 표시한다.
- `/business` 메인 카드의 이번 달 제출 요약은 기존 탐색 맥락을 유지하기 위해 변경하지 않는다.
- `npx tsc --noEmit`과 `npm run lint`는 종료 코드 0으로 통과했다. 린트에는 저장소 전반의 기존 경고만 남아 있고 이번 변경 파일의 새 경고는 없다.
- 모바일 지사 표는 첫 열만 `sticky`로 고정하고, 데스크톱에서는 기존 자동 너비와 일반 위치로 복귀시킨다.
- 모바일 지사명은 원문을 `title`에 유지하면서 화면에는 세 글자와 `...`를 표시한다.
- 표에 콘텐츠 기준 너비를 부여해 360px 화면에서도 스크롤 컨테이너가 실제 가로 오버플로를 갖도록 한다.
- 모바일 개선 후 `npx tsc --noEmit`과 `npm run lint`가 종료 코드 0으로 통과했다. 린트에는 기존 저장소 경고만 남아 있다.
- 지사별 월례보고 표의 현재 월 강조는 선택 연도가 현재 연도일 때만 적용해 과거·미래 연도의 같은 월을 오늘로 오인하지 않도록 한다.

---

# 품질 월례보고서 동일 공종 물량 동기화 컨텍스트 노트

- 피드백 위치는 월례보고서 행의 세 물량 입력칸이며, 요구 범위는 같은 공종으로 추가된 시험행의 연간·이번 달·다음 달 시공 물량 동기화다.
- 기존에는 콘크리트 슬럼프, 토공 일부 시험, 강관서포트·강관비계만 별도 규칙으로 숫자 입력을 전파한다.
- 실시대장 시험실적 연동으로 동일 공종의 새 시험항목 행이 자동 추가될 수 있으므로 특정 프리셋 이름이 아닌 동일 공종 기준으로 처리한다.
- 횟수·시험실적 열의 기존 공종별 규칙은 유지하고, 동일 공종 일반 전파는 첫 행에서 입력한 `yearlyPlan`, `monthVolume`, `nextMonthPlan`에 적용한다.
- 기존 미커밋 변경이 있는 `QualityTestRecordsTab.tsx`는 수정하지 않는다.
- 공종명 비교 시 앞뒤 공백을 제거하되 저장된 원문은 변경하지 않는다.
- `npx tsc --noEmit`과 `npm run lint`는 종료 코드 0으로 통과했다. 린트에는 저장소 전반의 기존 경고만 남아 있고 이번 변경 파일의 새 경고는 없다.
