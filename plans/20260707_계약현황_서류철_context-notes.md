# 컨텍스트 노트 — 계약(공사·용역) 현황 서류철

## 2026-07-07 페이지 피드백 반영 (11차) — 대표계약 체크 ↔ 편집 페이지 나라장터 연계 동기화

- **피드백**: "여기(대표 체크박스) 체크 했음에도 여기(편집 페이지 나라장터 연계 박스)에 안 뜨는데?" — 두 기능이 다른 컬럼을 써서 단절돼 있었음. 대표 체크는 `representative_contract_id`만 저장, 편집 박스(`G2bContractLookup`)는 `g2b_cntrct_no`/`g2b_ntce_no`만 읽음.
- **수정 ① 체크 시 g2b_* 동기화**: `handleToggleRepresentative`가 체크 시 `g2b_cntrct_no`(대표계약 `cntrct_no`)·`g2b_corp_nm`·`g2b_tot_amt`·`g2b_thtm_amt`를 함께 UPDATE. `g2b_ntce_no`는 NULL로 정리(이전 연계 계약의 공고번호가 남아 다른 계약을 가리키는 혼재 방지, project_contracts에는 공고번호 컬럼이 없음). **해제 시에는 연계를 지우지 않음** — 연계 계약이 기본 대표로 남는 기존 설계 유지. 대표계약과 편집 폼 연계는 같은 '프로젝트 대표 1건' 개념으로 통일.
- **수정 ② 편집 박스 자동 조회**: `G2bContractLookup`에 `initialNo`가 있으면 마운트 시 자동 `handleSearch()` — 조회 버튼을 누르기 전에도 연계 계약 카드가 바로 표시. `useRef` 가드로 StrictMode 이중 호출 방지. 등록 폼(`ProjectRegistrationForm`)은 `initialNo`를 안 넘겨 자동 조회 미발동(영향 없음).
- **백필**: 동기화 도입 이전에 체크된 3건(매화지구·모전리·오성1리)은 g2b가 null 또는 구 차수 번호 — `20260707-1804` 백필 SQL로 대표계약 값 일괄 동기화(사용자 실행 필요). 실행 안 해도 해당 프로젝트에서 체크 해제→재체크 2클릭으로 동일 효과.
- **수기 등록 계약(cntrct_no 없음)을 대표로 체크하면** g2b_cntrct_no가 null로 저장돼 편집 박스에는 여전히 안 뜸 — 나라장터 번호가 없는 계약은 조회 자체가 불가하므로 정상. 대표 표시는 `representative_contract_id`로 유지됨.

## 2026-07-07 페이지 피드백 반영 (10차) — 조달청 조회 탭 공사·용역 통합

- **피드백**: 조회 서브탭의 [공사 | 용역]을 하나로 합쳐 한 번에 조회. 탭 [공사 | 용역 | 계약번호] → **[공사·용역 | 계약번호]** (`LookupDiv: 'period' | 'byno'`, 기본 `'period'`). 탭 그리드 `grid-cols-3`→`grid-cols-2`.
- **구현**: API 오퍼레이션은 공사(`getCntrctInfoListCnstwkPPSSrch`)·용역(`getCntrctInfoListServcPPSSrch`)이 분리라 서버 라우트(`cntrct-list`)는 여전히 `div=cnstwk|servc` 단건만 받는다(변경 없음). 통합은 **클라이언트에서** `handleLookup`이 월마다 두 구분(`DIVS=['cnstwk','servc']`)을 `Promise.all`로 병렬 조회해 합치는 방식. 월 단위 동시성(CONCURRENCY 3)·진행률(`n/총 개월`)은 유지 — 한 달에 대해 공사·용역이 모두 끝나야 그 달 done +1, 둘 중 하나라도 실패하면 그 달 failCount +1.
- **결과 합치기는 기존 그대로**: `item.key`(계약명+체결일+총액+업체) dedupe → `latestPerContract`(차수 병합) → 체결일 내림차순. 공사/용역은 계약명·계약번호가 달라 충돌 없음. 모달 결과 목록·체크박스 그룹 로직도 이미 혼합 타입(계약번호 탭)을 처리하던 것이라 무변경.
- **`div=period`를 API로 보내지 않는다** — 초기 편집 흔적으로 프론트가 `div=period`를 보내 API가 400(`계약 구분 지정`)으로 거부하던 상태였으나, 클라이언트 팬아웃으로 전환해 해소. 서버 라우트 계약(단일 구분)은 그대로 두는 게 타임아웃·단일책임 측면에서 안전.

## 2026-07-07 페이지 피드백 반영 (9차) — 대표계약 1건 지정 컬럼

- **피드백**: 계약현황 목록에 "대표계약건 1건 설정을 위한 컬럼"을 넣고, 체크하면 대표 1건이 되도록. (편집 페이지 나라장터 연계의 "계약 1건 등록" 기능과 짝 — 여러 계약 중 프로젝트 대표 1건을 지정하는 용도.)
- **저장 위치**: `projects.representative_contract_id UUID REFERENCES project_contracts(id) ON DELETE SET NULL` (마이그레이션 `20260707-1730`, 사용자 실행 필요). 프로젝트에 단일 값으로 저장 → "1건" 단일 선택이 구조적으로 보장. 계약 행 삭제 시 자동 NULL.
- **왜 project_contracts의 boolean이 아니라 projects 컬럼인가**: ① 단일 선택을 값 하나로 강제(다른 행 clear 불필요). ② project_contracts UPDATE RLS는 `auth.uid()=created_by`라 남이 만든 행 clear가 실패할 수 있음 — projects UPDATE(편집 폼과 동일 경로)는 안전. 
- **그룹 단위 표시**: 목록은 차수 병합 그룹(`nameGroupKey`)이라, 저장된 id가 그룹 멤버 중 하나면 대표로 표시(`isGroupRepresentative`). 체크 시 그룹 대표행(`repr`) id 저장, 다시 체크 시 NULL. 업데이트로 새 최신 차수가 들어와 repr이 바뀌어도 이전 멤버 id가 그룹에 남아 대표 표시 유지(재체크 시 새 repr로 갱신).
- **UI**: 맨 앞 "대표" 체크박스 컬럼, 대표 행은 `bg-amber-50` 강조. 소계행 선두 `colSpan={3}→{4}`로 정렬 유지. 낙관적 업데이트+실패 롤백.
- **기본 대표(추가 요청)**: 명시적으로 저장된 대표(`representative_contract_id`)가 없으면, 편집 페이지에서 연계한 나라장터 계약(`projects.g2b_cntrct_no`/`g2b_ntce_no`)에 매칭되는 계약을 **기본 대표로 표시**(이미 등록된 계약이므로 자동 체크). `representativeId = representative_contract_id ?? g2bLinkedRepId`. 매칭은 `cntrct_no`(결합형 base 포함)·`unty_cntrct_no`·URL의 `ctrtNo`로. **DB에 쓰지 않는 표시용 폴백**이라 열람 전용 사용자(RLS로 projects UPDATE 불가)도 체크가 보이고, 사용자의 명시적 해제와 충돌하지 않음. 다른 계약을 체크하면 그 id가 저장돼 기본값을 덮어씀. 기본(연계) 계약은 저장값 null이라 클릭해도 null 유지 → 계속 체크(대표 1건 항상 유지).
- **merge_projects 영향 없음**: 이 FK는 `confrelid=project_contracts`(projects가 자식을 참조)라 함수의 자식 테이블 카운트(22, `confrelid=projects`)에 안 잡힘 — 갱신 불필요. 병합 시 source projects 행은 삭제되므로 target의 대표계약이 유지됨(source 선택은 폐기).
- **엑셀 내보내기 미반영**: 요청 범위 밖이라 대표 표기는 목록에만. 필요 시 export row에 플래그 추가로 확장 가능.

## 2026-07-07 페이지 피드백 반영 (8차) — 업데이트 버튼의 신규 연차(차수) 탐색

- **피드백**: "업데이트를 눌러도 과거 차수만 검색되고 금차년도(26년) 금액이 등록되지 않는다" + "계약명 링크가 25년 계약서로 간다".
- **원인 (실호출 확정, 모전지구)**: 장기계속계약의 연차별 차수 계약은 확정(R25TA→R26TA)·통합(R25TE→R26TE)·공고번호 조회 결과가 전부 분리 — 25년 차수의 unty(R25TE10097178)·공고번호(R25BK00660043) 어느 것으로 조회해도 26년 차수(R26TA0131651200, 금차 645,766,000)가 반환되지 않는다. 직전 커밋 1d240c8의 "통합계약번호 조회로 전 연차 획득" 전제가 오류. **번호 재조회로는 신규 연차를 절대 못 찾고, 기관명+계약명 월별 목록 조회(cntrct-list)가 유일한 발견 경로.**
- **수정**: `handleRefreshAll`을 2단계로 재구성. ① 기존 번호 재조회 갱신(같은 차수의 변경계약 반영) 유지, ② 신규 연차 탐색 신설 — 그룹별로 `금차 합 < 총액`(누락 차수 존재)일 때만, 최신 차수 체결월부터 이번 달까지(최대 24개월) cntrct-list를 월 단위 조회(동시 3). 기관명은 저장된 `cntrct_instt_nm` → `dminstt_nm` 첫 항목 → `guessInstName(managing_branch)` 폴백. 서버 계약명 필터는 원문 부분일치라 지구명인 첫 단어 토큰만 전송, 클라이언트에서 `nameGroupKey`(계약명 공백 제거+구분) 일치로 확정. `latestPerContract`로 변경차수 병합 후 미등록 건만 insert.
- **탐색 게이트가 `금차 합 < 총액`인 이유**: 단년 계약(총=금차)의 무의미한 스캔 방지. 신규 차수 등록 후 합이 총액과 같아지면 다음 업데이트부터 스캔이 자동으로 꺼진다. 합이 계속 모자라는 계약(용역 등 잔여 차수 남음)은 매번 짧은 구간(최신 체결월~현재)만 재스캔 — 자기제한적.
- **등록 키 정규화**: 계약명+체결일 키는 공백 제거(`nameDateKey`) — 실데이터에 계약명 뒤공백("…토목공사 ")이 있어 그대로 비교하면 목록 조회의 trim된 이름과 어긋난다. unty는 같은 계약에도 조회마다 변형 값(R26TE12021967/R26TE11137355 동시 실측)이라 식별자에서 계속 제외.
- **"링크가 25년 계약서" 피드백은 별도 수정 불필요** — 26년 차수가 등록되면 대표 행(repr, 최신 체결일)이 26년 차수가 되어 링크·체결일이 26년 기준으로 바뀐다.

## 2026-07-07 페이지 피드백 반영 (7차) — 연도 컬럼 헤더 축약·올해 강조

- 연도 컬럼 헤더 "25년 금차(원)" → **"25년"** (사용자 지정, '기타'는 "연도미상"). 금액 단위는 총계약금액(원) 컬럼으로 유추 가능.
- **올해 연도 컬럼은 amber 배경으로 강조** (사용자 지정) — 헤더 `bg-amber-100/70 text-amber-800`, 소계 `bg-amber-100/60`, 본문 `bg-amber-50/70`.

## 2026-07-07 페이지 피드백 반영 (6차) — 계약 단위 한 행 병합 표시

- **"장기계약건으로 등록하고 차수분은 연도 컬럼에만" 요청 반영.** 저장(DB)은 차수별 행 유지(조달청 원형·행별 업데이트 갱신 유지), **표시·집계만 계약 단위로 병합**하는 방식 채택 — 스키마 변경 없음.
- **그룹 키는 계약명(공백 제거)+구분(`nameGroupKey`).** 연차별 차수 계약은 확정·통합계약번호가 모두 달라(실측) 번호로 못 묶는다. 같은 프로젝트 서류철 안에서는 계약명 동일성이 안정적.
- **병합 행 표시 규칙**: 총액·체결일·계약상대자·수요기관·링크 = 최신 차수(체결일 기준, `repr`). 계약기간 = 멤버 최초 착수일 ~ 최종 준공일. 연도 컬럼 = 각 차수 금차가 각자 귀속 연도에 (`yearAmts`, 같은 연도 복수 차수는 합산). 차수 2건 이상이면 계약명 아래 "장기계속 · 차수 N건 병합" 표기.
- **소계 총액의 차수 중복 합산 버그도 함께 해소** — 기존엔 차수 행마다 같은 총액(예: 10,840,000×2)이 더해졌다. 이제 계약당 1회(repr)만 합산. 건수 표시(공사/용역·소계)도 계약 단위.
- **삭제는 그룹 단위** — 차수 포함 N건 confirm 후 `.in('id', members)` 일괄 삭제.
- **조회 모달 체크박스는 같은 계약명의 차수를 함께 토글** — 한 번 체크로 차수 전부 선택돼 "따로따로 등록" 불편 해소. 미등록 건만 대상.
- **기존 등록 건 주의**: `thtm_end_date`가 없는 구버전 행은 연도 폴백(진행 중=올해)이라 차수 2건이 같은 연도로 합산될 수 있음 — "업데이트" 버튼 한 번으로 금차준공일이 채워져 연도별로 분리된다.

## 2026-07-07 페이지 피드백 반영 (5차) — 장기계속계약 차수 구분

- **"같은 계약이 2건씩 조회" 원인은 취소·중복이 아니라 장기계속계약의 차수(연차)별 계약** (실호출 확정: 판부지구 재해예방기술지도 — 12월 건 R25TA0088725201 금차 1,320,000·금차준공 2025-12-20, 1월 건 R26TA0136334700 금차 9,520,000·금차준공 2026-12-20, 금차 합=총액 10,840,000). 계약번호가 서로 다른 별개 계약이라 `latestPerContract`(같은 계약의 변경차수 병합)에 안 걸리는 게 정상 — 걸러내면 안 됨.
- **API 필드**: 금차준공일은 공사 `thtmCcmpltDate`·용역 `thtmScmpltDate`, 장기계속구분은 공통 `lngtrmCtnuDivNm`(값: "신규"/"차수", 실호출 확인). 두 라우트(`cntrct-list`, `contract`) 모두 `thtmEndDate`·`lngtrmDiv`로 정규화해 반환 (additive — 기존 소비자 영향 없음).
- **모달 표시**: `lngtrmDiv === '차수'`면 계약명 옆 amber 배지 "장기계속(차수)" + title 툴팁. 메타 줄은 금차준공≠총준공일 때만 "금차준공 X · 총준공 Y"로 분리 표기 (같으면 기존 "준공 Y" 유지 — 단년 계약 노이즈 방지).
- **연도 귀속 규칙 변경**: `contractYear`는 `thtm_end_date`가 있으면 그 연도를 그대로 사용 (차수분 준공 연도 = 귀속 연도). 없는 행(직접 등록·구버전 등록 건)은 기존 폴백 유지 — 총준공 지난 계약은 준공 연도, 진행 중이면 올해. 이로써 12월 차수분 1,320,000이 25년 컬럼, 1월 차수분 9,520,000이 26년 컬럼으로 정확히 배치됨.
- **DB**: `project_contracts.thtm_end_date DATE` 추가 (`20260707-1503` 마이그레이션, 사용자 실행 필요). 등록·일괄 갱신(fill-if-present) 모두 저장. **마이그레이션 전에 배포하면 insert에 없는 컬럼이 포함돼 등록이 실패하므로 마이그레이션 먼저.**

## 2026-07-07 페이지 피드백 반영 (4차)

- **프로젝트 나라장터 연계 계약은 목록에 무조건 포함** (사용자 지정, 수정 폼 연계 박스 기준). 서류철 페이지 로드 시 `projects.g2b_cntrct_no/g2b_ntce_no`가 있고 목록에 매칭되는 행이 없으면 `/api/g2b/contract` 최신 차수(contracts[0])로 자동 등록. 모듈 스코프 `linkSyncTried` Set(`projectId|번호` 키)으로 StrictMode 이중 실행·재방문 중복 insert 방지 — 번호가 바뀌면 키가 달라져 재동기화됨. 실패는 조용히 무시(계약번호 탭 수동 등록 경로 존재).
- **차수(회차) 중복 노출 방지 — 조회는 무조건 최신 차수만** (사용자 확인 질문에서 도출). 기간 조회에서 원계약·변경계약이 다른 달에 걸리면 별개 행으로 수집되던 것을, 딥링크의 `ctrtNo`(계약 판별)+`ctrtChgOrd`(차수 비교, 폴백은 체결일)로 묶어 최신 차수만 남김(`latestPerContract`). 계약번호 조회 결과에도 동일 적용. 업데이트 버튼·연계 자동 등록은 원래 contracts[0](최신)만 사용.
- 조회 응답 매핑을 `contractRespToItem`/`itemToRow` 공용 헬퍼로 정리 (번호 조회·연계 자동 등록·선택 등록이 공유).

## 2026-07-07 페이지 피드백 반영 (3차)

- **계약번호 등록 건의 링크가 나라장터 홈으로 가던 문제**: `/api/g2b/contract`가 `cntrctInfoUrl`(홈)만 반환하고 상세 딥링크 `cntrctDtlInfoUrl`을 매핑하지 않았음. 라우트에 필드 추가(기존 소비자에 영향 없는 additive), 페이지는 딥링크 우선 사용. 홈만 가리키는 URL은 링크로 렌더하지 않는 방어 추가(`isDetailUrl`).
- **기등록 건이 조회에서 다시 선택되던 문제**: 같은 계약이라도 조회 경로(기간/번호)마다 `untyCntrctNo`가 다르고 목록 응답의 `dcsnCntrctNo`는 빈 값이 많아 번호 매칭이 놓침. **딥링크 URL의 `ctrtNo`(기본 계약번호, 차수·경로 무관 동일)를 판정 키로 추가**하고, 결합형 확정계약번호(13자리+)는 차수 2자리를 뗀 기본번호도 키로 등록.
- **업데이트(일괄 갱신) 버튼** (사용자 지정, 추가 버튼 좌측): 계약번호가 있는 등록 건을 순차로 `/api/g2b/contract` 재조회해 최신 차수 기준 금액·기간·업체·번호·딥링크를 갱신. 조회값이 빈 항목은 기존 값 유지(fill-if-present). RLS로 작성자 외 행은 갱신 불가 — 실패 목록에 표시. 홈 URL로 저장된 기존 행도 이 버튼으로 딥링크 보정됨 (service-role 직접 UPDATE는 권한 정책상 미실행).

## 2026-07-07 페이지 피드백 반영 (2차)

- **헤더 버튼 "조달청 조회" → "추가"** (사용자 지정). 모달 제목도 "계약 등록"으로.
- **모달 상위 탭 [직접 등록 | 조달청 조회] 신설** (사용자 지정). 기본 탭은 직접 등록 — 지급자재 등록 모달의 '직접 입력' 기본과 동일 관례.
- **조달청 조회 서브탭 [공사 | 용역 | 계약번호]** (사용자 지정). 계약번호 탭은 기존 `/api/g2b/contract` 재사용 — 확정계약번호·공고번호·통합계약번호 자동 판별, 공사/용역 구분은 응답 `bsnsDivNm`에 '용역' 포함 여부로 분류(물품·외자는 프로젝트 성격상 배제하고 공사로 폴백). 계약번호 결과에는 계약명 검색어 필터를 적용하지 않음(프리필 검색어가 단건 결과를 숨기는 사고 방지).
- **직접 등록 폼**: 구분/계약명(필수)/계약상대자/수요기관/총·금차계약금액(쉼표 자동)/체결·착수·준공일. 조달청 미등재 계약 대응.
- **표 소계행**: 헤더 아래 첫 행에 소계(N건) + 총액·금차 합계. 금차는 별도 컬럼 분리 (사용자 지정).
- 조회 결과 메타 줄에 준공일 표시 (공사 ttalCcmpltDate·용역 ttalScmpltDate — 목록 응답에 포함 실측 확인).

## 2026-07-07 페이지 피드백 반영 (1차)

- **검색어도 조회 조건으로 함께 적용** (사용자 피드백 "이것과 여기가 함께 조회 되어야지"). 계약현황 API는 납품요구 목록과 달리 **계약명 서버측 필터를 지원** — 공사 `cnstwkNm`, 용역 `cntrctNm` (실호출 확인: 여주.이천지사 3월 9건 → cnstwkNm=백사지구 1건). 단 **원문 부분일치**라 두 단어("백사지구 자동화")는 어순 불문 0건 — 그래서 검색어가 한 단어면 서버로 전송하고, 여러 단어면 서버 전송 없이 조회 후 클라이언트 단어별 OR 부분일치로 거른다 (건명 띄어쓰기 편차 대응 — 수불부 일괄 조회의 교훈 재사용).
- 기관명·검색어 입력란에 Enter로 조회 실행 추가.
- **API 응답 지연**: 계약현황 API는 첫 호출이 25초를 넘기는 경우가 실측돼 라우트 타임아웃을 40초로 상향 (납품요구 목록은 25초).
- **중복 행 dedupe 키는 표시 필드 기준** (계약명+체결일+총액+업체). untyCntrctNo 기준으로는 같은 계약이 번호 변형으로 중복 반환돼 걸러지지 않음 (실측: 여주.이천지사 3월 9행 → 8건).

## 2026-07-07 설계 결정

- **PPSSrch 일자 조회 파라미터는 `inqryBgnDate`/`inqryEndDate`** (실호출 확정). 번호 조회에서 쓰던 `inqryBgnDt` 형태나 `insttDivCd`+`insttNm` 조합, `dminsttNm` 등은 전부 resultCode 08 "필수값 입력 에러" 또는 무시. 기본 오퍼레이션(getCntrctInfoListCnstwk)은 반대로 `inqryBgnDt`(YYYYMMDDHHMM)를 받지만 기관 필터가 없어 채택하지 않음.
- **기관 필터는 `insttNm` 부분일치 하나뿐.** 계약기관·수요기관 어느 쪽 매칭인지 구분 파라미터 없음 — 농어촌공사 지사는 계약기관=수요기관이 일반적이라 실용상 문제 없음. "여주.이천지사"만으로 월 9건 수준.
- **다개월 범위는 API가 거부하지 않고 타임아웃**(40초+) — 수불부 일괄 조회처럼 클라이언트에서 월 단위 분할, 동시 3개 호출로 구현. 서버 라우트는 한 달만 처리 (같은 달 검증 포함).
- **등록 키는 `untyCntrctNo` 우선.** 신형 계약은 `dcsnCntrctNo`가 빈 값인 경우가 있어(실측) 등록됨 판정·dedupe는 untyCntrctNo → dcsnCntrctNo → 계약명+체결일 순 폴백.
- **목록 응답만으로 등록** — 수불부처럼 단건 상세 재조회가 필요 없음(목록 행에 금액·기간·업체 등 전부 포함). 선택 등록은 supabase 직접 insert.
- **공사 → 용역 표시 순서** (사용자 지정): 표 정렬은 contract_type '공사' 우선, 그 안에서 체결일 오름차순.
- **기간 프리셋은 기간 설정만** — 수불부 일괄 조회의 최종 결정(9d41960)을 그대로 따름. 조회는 조회 버튼으로만.
- **기타 캐비넷에 "P (계획)" 점선 박스 신설.** 기존에는 "C (점검)" 박스(방문일지)뿐인데 계약 현황은 점검 서류가 아니라서 계획 그룹을 새로 만듦. bottomLabel은 "계약".
- **수기 등록·수정 폼은 이번 범위에서 제외** — 사용자 요청이 "조달청에서 불러올 수 있게"라서 조회 등록 + 삭제만. 금액 등 보정 필요 시 후속.
- 조달청 계약현황 목록의 **같은 계약 중복 행**(untyCntrctNo 변형)은 라우트에서 dedupe.
- DATA_GO_KR_API_KEY는 Vercel Preview/Production에 등록 확인 (2026-07-06자 등록, vercel env ls 확인).

## 2026-07-07 UI 피드백 반영 (계약 등록 모달)

- **등록 모달 기본 탭은 '조달청 조회'.** modalTab 초기값을 'g2b'로 변경하고, openLookup()에서 열 때마다 'g2b'로 재설정 — 직접 등록 탭에 두고 닫아도 다음 열기에는 조달청 조회가 활성화 (사용자 피드백 "매번 조달청이 활성화").
- **구분 탭(공사/용역/계약번호)과 아래 조회 조건을 border-blue-200 테두리 박스로 묶음** — 탭 선택에 따라 바뀌는 입력 필드들이 한 묶음임이 보이도록 (사용자 피드백 "배경선"). 조회 결과·에러 표시는 박스 밖 유지.
- **구분 탭을 '공사'·'용역' 2개 → '공사·용역' 1개로 통합** (탭은 '공사·용역', '계약번호' 2개). `LookupDiv`를 `'period' | 'byno'`로 바꾸고 기본값 `'period'`. 사용자가 공사/용역을 나눠 조회할 필요 없이 기간 조회 한 번으로 둘 다 받게 함.
  - **탭 그리드 `grid-cols-3` → `grid-cols-2`** — 탭이 2개로 줄었는데 3열 그리드가 남아 빈 열이 생기던 것을 정리 (사용자가 중단하고 넘긴 지점).
  - **`handleLookup`은 월마다 cnstwk·servc 두 오퍼레이션을 모두 호출해 합침** — 조달청 계약현황 API는 공사(`getCntrctInfoListCnstwkPPSSrch`)·용역(`getCntrctInfoListServcPPSSrch`) 오퍼레이션이 분리라 `div=period` 단일 호출은 라우트에서 400. `DIVS=['cnstwk','servc']`를 월 태스크 안에서 `Promise.all`로 조회. 진행률(`done/total`)은 "개월" 단위 유지 위해 월당 1회만 증가, `failCount`는 두 구분 모두 실패한 월만 카운트(`failCount===months.length` 전량 실패 판정과 정합). 결과는 기존 `key` dedupe + `latestPerContract`로 공사·용역 섞여도 합쳐짐.
  - **업데이트/신규 차수 탐색 경로(scanPlans, line~800)의 `div`는 각 계약의 `contract_type`으로 그대로 유도** — 탭 통합과 무관(이미 등록된 계약은 구분이 정해져 있음). 변경 없음.
