# 사고 통계 분석 체크리스트

## 설계와 기록

- [x] 기존 카드·라우팅 패턴을 확인한다.
- [x] 사고 초안 스키마와 현행 권한 불일치를 확인한다.
- [x] 분석 점검 3종과 통계 기준을 확정한다.
- [x] 권한과 사고 범위를 사용자에게 확인한다.
- [x] 계획서와 컨텍스트 노트를 작성한다.

## 데이터베이스와 데이터 계층

- [x] `project_accidents` 마이그레이션과 제약조건을 추가한다.
- [x] 관할 조회와 본부급 쓰기 RLS를 추가한다.
- [x] `merge_projects` 이전 목록과 25개 가드를 갱신한다.
- [x] 프로젝트 삭제 연관 건수에 사고 이력을 추가한다.
- [x] 사고 CRUD와 3종 점검 조회 함수를 구현한다.
- [x] 점검 공통 DTO와 프로젝트-월 분석 계산을 구현한다.
- [x] 준공 프로젝트의 과거 이력과 공사기간 밖 실제 기록 월이 분석에 포함되는지 확인한다.

## UI와 라우팅

- [x] `/safe` 카드 그리드에 사고 통계 분석 카드를 추가한다.
- [x] 루트·지사 사고 분석 라우트를 추가한다.
- [x] Dashboard 경로 인식과 상세 뷰 연결을 추가한다.
- [x] 기간·조직·프로젝트·사고 필터를 구현한다.
- [x] KPI, 점검구간 관계, 월별 추이, 프로젝트 요약, 사고 이력표를 구현한다.
- [x] 사고 입력·수정 모달과 삭제 확인을 구현한다.
- [x] 지사급 조회 전용과 본부급 관리 UI·RLS 분기를 코드로 검증한다.
- [x] 로딩·오류·빈 상태와 인과관계 주의문을 구현한다.

## 검증과 마무리

- [x] React 컴포넌트 품질 체크리스트를 적용한다.
- [x] `npm run lint`를 통과한다.
- [x] `npx tsc --noEmit`를 통과한다.
- [x] `git diff --check`를 통과한다.
- [ ] 발주청 로그인 상태로 카드·필터·CRUD·권한·반응형을 브라우저에서 끝까지 확인한다.
- [x] 별도 개발 서버에서 `/safe`, `/safe/accident-analysis` 컴파일과 HTTP 200을 확인한다.
- [x] diff에서 무관한 변경과 기존 사용자 파일이 섞이지 않았는지 확인한다.
- [x] `docs/architecture.md`, `docs/database.md`를 현재 동작과 맞춘다.
- [x] 의미 단위 커밋을 작성한다.

브라우저 전체 시나리오는 3001번 검증 서버에 발주청 로그인 상태가 없어 보류했다. 기존 3000번 개발 서버는 작업 코드와 별개로 HTTP 500 상태이며, 별도 3001번 서버에서는 두 경로가 정상 컴파일·응답했다. 원격 데이터베이스에는 마이그레이션을 아직 적용하지 않았다.
