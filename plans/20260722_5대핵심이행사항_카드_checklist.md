# 5대 핵심이행사항 카드 체크리스트

## Task A — FiveKeyStatusView (codex)

- [x] `src/components/dashboard/FiveKeyStatusView.tsx` 신규 생성 (첫 줄 한국어 역할 주석)
- [x] Props 계약 준수 (`initialHq`, `initialBranch`, `onBack`)
- [x] `getProjectsByUserBranch` + `headquarters_inspections` 직접 조회 (필요 컬럼만 select)
- [x] 빈 five_key_items·전체 grade '' 행은 미점검 처리 (기본값 채움 금지)
- [x] 카테고리 대표 등급 = 최악값, N/A·횟수 항목 처리
- [x] 요약 3칸 + 본부·지사 필터 + 등급 뱃지 테이블
- [x] 행 클릭 → `/project/{id}/headquarters-inspection?fromBranch=` 이동

## Task B — Dashboard 배선·라우트 (grok)

- [x] `src/app/safe/five-key/page.tsx` 생성 (headquarters/page.tsx 패턴)
- [x] `src/app/safe/branch/[branch]/five-key/page.tsx` 생성
- [x] `Dashboard.tsx` 516행·528행 카드명 목록에 `'five-key'` 추가
- [x] `FiveKeyStatusView` import 추가
- [x] 카드 그리드에 카드 추가 (법적이행확인 다음, 사고 통계 앞, ShieldCheck·emerald)
- [x] `selectedSafetyCard === 'five-key'` 뷰 렌더 분기 (legal-compliance 분기 미러)

## 검수 (코디네이터)

- [x] 두 워커 diff 직접 확인 (스펙 외 변경 없음)
- [x] 정렬부 `managing_branch` null 방어 1줄 직접 보정
- [x] `npx tsc --noEmit` 통과 (오류 0)
- [x] ESLint 신규 파일 3개 무경고 (Dashboard.tsx 경고 17건은 전부 기존 것)
- [x] 의미 단위 커밋
