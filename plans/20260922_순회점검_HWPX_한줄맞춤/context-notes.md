# 컨텍스트 노트 — 순회점검 HWPX 한 줄 맞춤

- 점검사항 셀(표1 3~15행 1열, colSpan 3) 폭 38236, cellMargin 510/510 → lineseg horzsize 37216.
- 점검사항 charPr 18 = 11pt, fontRef hangul 1 / latin 0, ratio 100, spacing 0. 문단 paraPr 0(3~7행)·23(8~15행) 모두 JUSTIFY·KEEP_WORD.
- 성명 셀(표2 2행 6열) paraPr 24 = RIGHT, 줄간격 160%. paraPr 22 = CENTER, 줄간격 160%로 동일하므로 새 paraPr 없이 교체.
- 기존 테스트 "셀 그리드·문단 서식" 단언은 모든 셀 paraPrIDRef가 원본과 같다고 보므로 (2,6) 예외가 필요하다.
- header 테스트는 id 26 이상 charPr을 복제본으로 보고 걷어낸 뒤 원본과 비교한다. 장평/자간 복제본도 같은 규칙에 들어간다.
- 한글 장평 범위 50~200%, 자간 −50~50%.
- 실측 보정(2026-09-22, 한글 2022 PDF). 추정 폭 모델은 원본 서식(장평 100·자간 0)에서는 정확하지만, 장평을 줄일수록 실제 폭이 모델보다 넓다(장평 73%에서 약 2%, 55%에서 약 4%). 자간 −5의 효과는 모델(글자당 5% em)의 절반 정도다. 그래서 장평은 자간 효과를 빼고 여유 5%로 계산하고 자간 −5를 고정한다. 47~74자(혼합 포함) 표본 전부 한 줄에 들어갔다.
- 장평 50% 하한에 걸리는 84자 표본은 자간 −15로 한 줄에 들어갔다.
- 실험 파일은 `safesys-app/scratch/patrol-ledger/`(gitignore)에 있다. `build-long.mjs`가 긴 문장 샘플 hwpx를 만든다.
