# 관리자 Command Center UI 개편 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/admin/users`와 `/admin/projects`를 Midnight Command 스타일의 반응형 관리자 콘솔로 개편하고, 모든 데이터 열의 3단계 정렬·모바일 접기/펼치기·우측 상단 로그아웃을 제공한다.

**Architecture:** 기존 관리자 API와 페이지별 데이터 요청·관리 작업은 유지한다. 정렬 상태와 값 비교는 순수 함수 모듈로, 접근 가능한 정렬 컨트롤과 모바일 disclosure는 공용 컴포넌트로 분리하며, 각 페이지는 필터·페이지·펼친 행 상태와 자신의 열 정의만 소유한다.

**Tech Stack:** Next.js 15 App Router, React 19, TypeScript strict mode, Tailwind CSS 4, Lucide React, Supabase Auth, Playwright Test.

## Global Constraints

- 구현 작업은 저장소 규칙에 따라 `worker-opus` 워커에게 위임하고 Advisor가 diff와 테스트를 직접 검증한다.
- 새 SQL 마이그레이션, API 응답 변경, 데이터 모델 변경, 신규 패키지 설치를 하지 않는다.
- 기존 가입자 인증·수정·삭제와 프로젝트 조회·삭제 API를 그대로 사용한다.
- 새 소스 파일 첫 줄에는 역할을 설명하는 한 줄 한국어 주석을 작성한다.
- 작업 열을 제외한 모든 데이터 열은 `정렬 없음 → 오름차순 → 내림차순 → 정렬 없음` 순환을 지원한다.
- 데이터 처리 순서는 `검색·필터 → 전체 결과 안정 정렬 → 페이지 분할`로 고정한다.
- 767px 이하에서는 접힌 목록을 먼저 표시하고 한 번에 한 항목만 상세로 펼친다.
- 로그아웃 버튼은 데스크톱과 모바일 우측 상단에 표시하며 성공 시 `/login`으로 이동한다.
- `npm run build`는 사용자 동의 없이 실행하지 않는다.
- 모든 명령은 `safesys-app` 디렉터리에서 실행한다.

---

## 파일 구조

- Create `safesys-app/src/lib/admin-sort.ts` — 범용 정렬 상태, 3단계 전환, null-last 안정 정렬.
- Create `safesys-app/src/lib/admin-list-sort.ts` — 가입자·프로젝트 열 키와 값 추출기.
- Create `safesys-app/src/lib/admin-session.ts` — 테스트 가능한 관리자 로그아웃 실행 함수.
- Create `safesys-app/src/components/admin/AdminSortControls.tsx` — 데스크톱 정렬 헤더와 모바일 정렬 컨트롤.
- Create `safesys-app/src/components/admin/AdminMobileDisclosure.tsx` — 모바일 접힌 목록과 상세 영역의 접근 가능한 공통 셸.
- Modify `safesys-app/src/app/admin/layout.tsx` — Midnight Command 셸, 반응형 내비게이션, 로그아웃.
- Modify `safesys-app/src/app/admin/users/page.tsx` — 가입자 정렬, 새 디자인, 모바일 disclosure.
- Modify `safesys-app/src/app/admin/projects/page.tsx` — 프로젝트 정렬, 새 디자인, 모바일 disclosure.
- Create `safesys-app/tests/admin-sort.spec.ts` — 정렬 상태·비교·열 값 매핑 테스트.
- Create `safesys-app/tests/admin-controls.spec.tsx` — 정렬 컨트롤과 disclosure의 접근성 마크업 테스트.
- Create `safesys-app/tests/admin-session.spec.ts` — 로그아웃 성공·실패 흐름 테스트.

---

### Task 1: 공용 정렬 도메인

**Files:**

- Create `safesys-app/tests/admin-sort.spec.ts`.
- Create `safesys-app/src/lib/admin-sort.ts`.
- Create `safesys-app/src/lib/admin-list-sort.ts`.

**Interfaces:**

- Produces `SortState<K>`, `SortDirection`, `SortValue`, `nextSortState`, `sortRows`.
- Produces `AdminUserSortKey`, `AdminProjectSortKey`, `ADMIN_USER_SORT_OPTIONS`, `ADMIN_PROJECT_SORT_OPTIONS`, `getAdminUserSortValue`, `getAdminProjectSortValue`.
- Consumers are Tasks 2, 4, and 5.

- [ ] **Step 1: 정렬 요구사항을 표현하는 실패 테스트 작성**

```ts
// 관리자 목록의 공용 정렬 규칙과 열 값 매핑을 검증하는 테스트
import { expect, test } from '@playwright/test'
import { nextSortState, sortRows, type SortState } from '../src/lib/admin-sort'
import {
  getAdminProjectSortValue,
  getAdminUserSortValue,
} from '../src/lib/admin-list-sort'

test('같은 열은 없음, 오름차순, 내림차순, 없음 순으로 순환한다', () => {
  const initial: SortState<'name'> = { key: null, direction: null }
  const ascending = nextSortState(initial, 'name')
  const descending = nextSortState(ascending, 'name')
  const cleared = nextSortState(descending, 'name')

  expect(ascending).toEqual({ key: 'name', direction: 'asc' })
  expect(descending).toEqual({ key: 'name', direction: 'desc' })
  expect(cleared).toEqual(initial)
})

test('다른 열을 선택하면 오름차순으로 전환한다', () => {
  expect(nextSortState({ key: 'name', direction: 'desc' }, 'email')).toEqual({
    key: 'email',
    direction: 'asc',
  })
})

test('빈 값은 양 방향에서 마지막이고 동일 값은 원본 순서를 유지한다', () => {
  const rows = [
    { id: 'b', value: 2 },
    { id: 'same-1', value: 1 },
    { id: 'empty', value: null },
    { id: 'same-2', value: 1 },
  ]

  expect(sortRows(rows, { key: 'value', direction: 'asc' }, (row) => row.value).map((row) => row.id))
    .toEqual(['same-1', 'same-2', 'b', 'empty'])
  expect(sortRows(rows, { key: 'value', direction: 'desc' }, (row) => row.value).map((row) => row.id))
    .toEqual(['b', 'same-1', 'same-2', 'empty'])
})

test('한국어 문자열은 숫자를 포함해 자연스러운 순서로 정렬한다', () => {
  const rows = [{ name: '현장10' }, { name: '가설공사' }, { name: '현장2' }]
  expect(sortRows(rows, { key: 'name', direction: 'asc' }, (row) => row.name).map((row) => row.name))
    .toEqual(['가설공사', '현장2', '현장10'])
})

test('필터링된 전체 결과를 정렬한 뒤 페이지를 나눈다', () => {
  const rows = [
    { id: 'older', active: true, createdAt: 1 },
    { id: 'newer', active: true, createdAt: 3 },
    { id: 'inactive', active: false, createdAt: 4 },
    { id: 'middle', active: true, createdAt: 2 },
  ]
  const filtered = rows.filter((row) => row.active)
  const sorted = sortRows(filtered, { key: 'createdAt', direction: 'desc' }, (row) => row.createdAt)
  expect(sorted.slice(0, 2).map((row) => row.id)).toEqual(['newer', 'middle'])
})

test('가입자 열을 한국어 문자열, 날짜, 인증 상태 값으로 변환한다', () => {
  const user = {
    full_name: '김현우',
    email: 'kim@example.com',
    role: '발주청',
    hq_division: '경기본부',
    branch_division: '수원지사',
    company_name: '한국안전건설',
    position: '안전관리자',
    created_at: '2026-08-01T00:00:00.000Z',
    last_sign_in_at: null,
    email_confirmed_at: '2026-08-02T00:00:00.000Z',
  }

  expect(getAdminUserSortValue(user, 'organization')).toBe('경기본부 수원지사')
  expect(getAdminUserSortValue(user, 'createdAt')).toBe(Date.parse(user.created_at))
  expect(getAdminUserSortValue(user, 'lastSignInAt')).toBeNull()
  expect(getAdminUserSortValue(user, 'confirmed')).toBe(true)
})

test('프로젝트 열을 등록자, 날짜, 활성 상태 값으로 변환한다', () => {
  const project = {
    project_name: '광역철도 제2공구',
    managing_hq: '경기본부',
    managing_branch: '수원지사',
    created_at: '2026-07-28T00:00:00.000Z',
    isActive: false,
    creator: { fullName: '김현우', companyName: '한국안전건설' },
  }

  expect(getAdminProjectSortValue(project, 'creator')).toBe('김현우 한국안전건설')
  expect(getAdminProjectSortValue(project, 'createdAt')).toBe(Date.parse(project.created_at))
  expect(getAdminProjectSortValue(project, 'status')).toBe(false)
})
```

- [ ] **Step 2: 테스트를 실행해 RED 확인**

Run `npx playwright test tests/admin-sort.spec.ts --project=chromium`.

Expected result is FAIL because `src/lib/admin-sort.ts` and `src/lib/admin-list-sort.ts` do not exist.

- [ ] **Step 3: 최소 정렬 도메인 구현**

```ts
// 관리자 목록에서 공유하는 3단계 안정 정렬 규칙을 제공한다
export type SortDirection = 'asc' | 'desc'
export type SortValue = string | number | boolean | null | undefined

export interface SortState<K extends string> {
  key: K | null
  direction: SortDirection | null
}

export function nextSortState<K extends string>(
  current: SortState<K>,
  key: K
): SortState<K> {
  if (current.key !== key || current.direction === null) return { key, direction: 'asc' }
  if (current.direction === 'asc') return { key, direction: 'desc' }
  return { key: null, direction: null }
}

function hasValue(value: SortValue): value is string | number | boolean {
  return value !== null && value !== undefined && value !== '' &&
    !(typeof value === 'number' && Number.isNaN(value))
}

function compareValues(left: Exclude<SortValue, null | undefined>, right: Exclude<SortValue, null | undefined>): number {
  if (typeof left === 'number' && typeof right === 'number') return left - right
  if (typeof left === 'boolean' && typeof right === 'boolean') return Number(left) - Number(right)
  return String(left).localeCompare(String(right), 'ko-KR', { numeric: true, sensitivity: 'base' })
}

export function sortRows<T, K extends string>(
  rows: readonly T[],
  state: SortState<K>,
  getValue: (row: T, key: K) => SortValue
): T[] {
  if (!state.key || !state.direction) return [...rows]

  const key = state.key
  const direction = state.direction
  return rows
    .map((row, index) => ({ row, index }))
    .sort((left, right) => {
      const leftValue = getValue(left.row, key)
      const rightValue = getValue(right.row, key)
      const leftHasValue = hasValue(leftValue)
      const rightHasValue = hasValue(rightValue)
      if (!leftHasValue || !rightHasValue) {
        if (!leftHasValue && !rightHasValue) return left.index - right.index
        return leftHasValue ? -1 : 1
      }
      const compared = compareValues(leftValue, rightValue)
      return compared === 0 ? left.index - right.index : compared * (direction === 'asc' ? 1 : -1)
    })
    .map(({ row }) => row)
}
```

`admin-list-sort.ts`에는 다음의 완전한 열 키와 옵션을 선언한다.

```ts
// 가입자와 프로젝트 관리자 목록의 정렬 열 및 값 추출기를 정의한다
import type { SortValue } from '@/lib/admin-sort'

export type AdminUserSortKey =
  | 'name' | 'email' | 'role' | 'organization' | 'company'
  | 'position' | 'createdAt' | 'lastSignInAt' | 'confirmed'
export type AdminProjectSortKey =
  | 'name' | 'hq' | 'branch' | 'creator' | 'createdAt' | 'status'

export const ADMIN_USER_SORT_OPTIONS = [
  { key: 'name', label: '이름' },
  { key: 'email', label: '이메일' },
  { key: 'role', label: '역할' },
  { key: 'organization', label: '본부·지사' },
  { key: 'company', label: '회사' },
  { key: 'position', label: '직책' },
  { key: 'createdAt', label: '가입일' },
  { key: 'lastSignInAt', label: '최근 로그인' },
  { key: 'confirmed', label: '인증 상태' },
] as const

export const ADMIN_PROJECT_SORT_OPTIONS = [
  { key: 'name', label: '프로젝트명' },
  { key: 'hq', label: '본부' },
  { key: 'branch', label: '지사' },
  { key: 'creator', label: '등록자' },
  { key: 'createdAt', label: '등록일' },
  { key: 'status', label: '상태' },
] as const

type UserSortRecord = {
  full_name: string | null; email: string | null; role: string | null
  hq_division: string | null; branch_division: string | null
  company_name: string | null; position: string | null; created_at: string
  last_sign_in_at: string | null; email_confirmed_at: string | null
}

type ProjectSortRecord = {
  project_name: string; managing_hq: string; managing_branch: string
  created_at: string; isActive: boolean
  creator: { fullName: string; companyName: string } | null
}

function timestamp(value: string | null): number | null {
  if (!value) return null
  const parsed = Date.parse(value)
  return Number.isNaN(parsed) ? null : parsed
}

export function getAdminUserSortValue(row: UserSortRecord, key: AdminUserSortKey): SortValue {
  const values: Record<AdminUserSortKey, SortValue> = {
    name: row.full_name,
    email: row.email,
    role: row.role,
    organization: [row.hq_division, row.branch_division].filter(Boolean).join(' ') || null,
    company: row.company_name,
    position: row.position,
    createdAt: timestamp(row.created_at),
    lastSignInAt: timestamp(row.last_sign_in_at),
    confirmed: Boolean(row.email_confirmed_at),
  }
  return values[key]
}

export function getAdminProjectSortValue(row: ProjectSortRecord, key: AdminProjectSortKey): SortValue {
  const values: Record<AdminProjectSortKey, SortValue> = {
    name: row.project_name,
    hq: row.managing_hq,
    branch: row.managing_branch,
    creator: row.creator ? `${row.creator.fullName} ${row.creator.companyName}`.trim() : null,
    createdAt: timestamp(row.created_at),
    status: row.isActive,
  }
  return values[key]
}
```

- [ ] **Step 4: 정렬 테스트 GREEN 확인**

Run `npx playwright test tests/admin-sort.spec.ts --project=chromium`.

Expected result is `7 passed`.

- [ ] **Step 5: 타입 검사와 커밋**

Run `npx tsc --noEmit` and expect exit code 0.

```bash
git add src/lib/admin-sort.ts src/lib/admin-list-sort.ts tests/admin-sort.spec.ts
git commit -m "feat: 관리자 목록 정렬 도메인 추가"
```

---

### Task 2: 접근 가능한 공용 정렬·모바일 목록 컴포넌트

**Files:**

- Create `safesys-app/tests/admin-controls.spec.tsx`.
- Create `safesys-app/src/components/admin/AdminSortControls.tsx`.
- Create `safesys-app/src/components/admin/AdminMobileDisclosure.tsx`.

**Interfaces:**

- Consumes `SortState<K>`, `SortDirection` from Task 1.
- Produces `SortableHeader<K>`, `MobileSortControls<K>`, `AdminMobileDisclosure`.
- Consumers are Tasks 4 and 5.

- [ ] **Step 1: 접근성 마크업 실패 테스트 작성**

```tsx
// 관리자 정렬 컨트롤과 모바일 상세 목록의 접근성 마크업을 검증한다
import { createElement } from 'react'
import { renderToStaticMarkup } from 'react-dom/server'
import { expect, test } from '@playwright/test'
import { MobileSortControls, SortableHeader } from '../src/components/admin/AdminSortControls'
import { AdminMobileDisclosure } from '../src/components/admin/AdminMobileDisclosure'

test('활성 정렬 헤더는 aria-sort와 현재 방향을 표시한다', () => {
  const markup = renderToStaticMarkup(createElement('table', null,
    createElement('thead', null, createElement('tr', null,
      createElement(SortableHeader, {
        label: '이름', sortKey: 'name', sortState: { key: 'name', direction: 'asc' }, onSort: () => undefined,
      })
    ))
  ))
  expect(markup).toContain('aria-sort="ascending"')
  expect(markup).toContain('이름 오름차순 정렬 중')
})

test('모바일 정렬 컨트롤은 기본 순서와 모든 열을 제공한다', () => {
  const markup = renderToStaticMarkup(createElement(MobileSortControls, {
    options: [{ key: 'name', label: '이름' }, { key: 'email', label: '이메일' }],
    sortState: { key: null, direction: null },
    onChange: () => undefined,
  }))
  expect(markup).toContain('정렬 기준')
  expect(markup).toContain('기본 순서')
  expect(markup).toContain('이메일')
})

test('모바일 목록은 닫힘과 펼침 상태를 aria-expanded로 구분한다', () => {
  const collapsed = renderToStaticMarkup(createElement(AdminMobileDisclosure, {
    id: 'user-1', expanded: false, onToggle: () => undefined,
    summary: createElement('span', null, '김현우'),
    children: createElement('span', null, '경기본부'),
  }))
  const expanded = renderToStaticMarkup(createElement(AdminMobileDisclosure, {
    id: 'user-1', expanded: true, onToggle: () => undefined,
    summary: createElement('span', null, '김현우'),
    children: createElement('span', null, '경기본부'),
  }))

  expect(collapsed).toContain('aria-expanded="false"')
  expect(collapsed).not.toContain('경기본부')
  expect(expanded).toContain('aria-expanded="true"')
  expect(expanded).toContain('경기본부')
})
```

- [ ] **Step 2: 테스트 RED 확인**

Run `npx playwright test tests/admin-controls.spec.tsx --project=chromium`.

Expected result is FAIL because both shared components are absent.

- [ ] **Step 3: 공용 컴포넌트 최소 구현**

`AdminSortControls.tsx`는 다음 API와 동작을 구현한다.

```tsx
// 관리자 목록의 데스크톱·모바일 정렬 조작 UI를 제공한다
'use client'

import { ArrowDown, ArrowUp, ArrowUpDown } from 'lucide-react'
import type { SortDirection, SortState } from '@/lib/admin-sort'

type SortOption<K extends string> = { key: K; label: string }

export function SortableHeader<K extends string>({
  label, sortKey, sortState, onSort, className = '',
}: {
  label: string; sortKey: K; sortState: SortState<K>; onSort: (key: K) => void; className?: string
}) {
  const active = sortState.key === sortKey
  const ariaSort = !active ? 'none' : sortState.direction === 'asc' ? 'ascending' : 'descending'
  const directionLabel = !active ? '정렬 안 됨' : sortState.direction === 'asc' ? '오름차순 정렬 중' : '내림차순 정렬 중'
  const Icon = !active ? ArrowUpDown : sortState.direction === 'asc' ? ArrowUp : ArrowDown

  return (
    <th scope="col" aria-sort={ariaSort} className={className}>
      <button type="button" onClick={() => onSort(sortKey)} aria-label={`${label} ${directionLabel}`}
        className="group inline-flex min-h-10 w-full items-center gap-1.5 rounded-lg px-3 py-2 text-left font-semibold text-slate-600 outline-none transition hover:bg-slate-100 hover:text-slate-950 focus-visible:ring-2 focus-visible:ring-blue-500">
        <span>{label}</span><Icon className={`h-3.5 w-3.5 ${active ? 'text-blue-600' : 'text-slate-400 group-hover:text-slate-600'}`} />
      </button>
    </th>
  )
}

export function MobileSortControls<K extends string>({
  options, sortState, onChange,
}: {
  options: readonly SortOption<K>[]; sortState: SortState<K>; onChange: (state: SortState<K>) => void
}) {
  return (
    <div className="grid grid-cols-[minmax(0,1fr)_132px] gap-2 md:hidden">
      <label className="grid gap-1 text-xs font-semibold text-slate-600">
        <span>정렬 기준</span>
        <select value={sortState.key ?? ''} onChange={(event) => {
          const key = event.target.value as K
          onChange(key ? { key, direction: sortState.key === key && sortState.direction ? sortState.direction : 'asc' } : { key: null, direction: null })
        }} className="h-11 rounded-xl border border-slate-200 bg-white px-3 text-sm text-slate-800 outline-none focus:border-blue-400 focus:ring-2 focus:ring-blue-100">
          <option value="">기본 순서</option>
          {options.map((option) => <option key={option.key} value={option.key}>{option.label}</option>)}
        </select>
      </label>
      <label className="grid gap-1 text-xs font-semibold text-slate-600">
        <span>정렬 방향</span>
        <select value={sortState.direction ?? 'asc'} disabled={!sortState.key}
          onChange={(event) => onChange({ key: sortState.key, direction: event.target.value as SortDirection })}
          className="h-11 rounded-xl border border-slate-200 bg-white px-3 text-sm text-slate-800 outline-none disabled:bg-slate-100 disabled:text-slate-400 focus:border-blue-400 focus:ring-2 focus:ring-blue-100">
          <option value="asc">오름차순</option><option value="desc">내림차순</option>
        </select>
      </label>
    </div>
  )
}
```

`AdminMobileDisclosure.tsx`는 다음을 구현한다.

```tsx
// 모바일 관리자 목록의 접힌 요약과 펼친 상세 영역을 제공한다
'use client'

import type { ReactNode } from 'react'
import { ChevronDown } from 'lucide-react'

export function AdminMobileDisclosure({ id, expanded, onToggle, summary, children }: {
  id: string; expanded: boolean; onToggle: () => void; summary: ReactNode; children: ReactNode
}) {
  const contentId = `admin-mobile-detail-${id}`
  return (
    <article className={`overflow-hidden rounded-2xl border bg-white shadow-sm transition ${expanded ? 'border-blue-300 ring-2 ring-blue-100' : 'border-slate-200'}`}>
      <button type="button" aria-expanded={expanded} aria-controls={contentId} onClick={onToggle}
        className="flex min-h-[72px] w-full items-center gap-3 px-4 py-3 text-left outline-none focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-blue-500">
        <div className="min-w-0 flex-1">{summary}</div>
        <ChevronDown className={`h-5 w-5 shrink-0 text-slate-400 transition-transform ${expanded ? 'rotate-180 text-blue-600' : ''}`} aria-hidden="true" />
      </button>
      {expanded && <div id={contentId} className="border-t border-slate-100 px-4 py-4">{children}</div>}
    </article>
  )
}
```

- [ ] **Step 4: 공용 컴포넌트 테스트 GREEN 확인**

Run `npx playwright test tests/admin-controls.spec.tsx --project=chromium`.

Expected result is `3 passed`.

- [ ] **Step 5: 타입 검사와 커밋**

Run `npx tsc --noEmit` and expect exit code 0.

```bash
git add src/components/admin/AdminSortControls.tsx src/components/admin/AdminMobileDisclosure.tsx tests/admin-controls.spec.tsx
git commit -m "feat: 관리자 반응형 목록 컨트롤 추가"
```

---

### Task 3: Midnight Command 공통 레이아웃과 로그아웃

**Files:**

- Create `safesys-app/tests/admin-session.spec.ts`.
- Create `safesys-app/src/lib/admin-session.ts`.
- Modify `safesys-app/src/app/admin/layout.tsx:1-90`.

**Interfaces:**

- Produces `performAdminSignOut(signOut, redirect): Promise<void>`.
- Keeps the existing `/api/admin/me` guard and OTP checks unchanged.
- Provides desktop sidebar, mobile tab bar, global top-right logout, and content shell consumed automatically by Tasks 4 and 5.

- [ ] **Step 1: 로그아웃 흐름 실패 테스트 작성**

```ts
// 관리자 로그아웃의 성공 이동과 실패 차단을 검증한다
import { expect, test } from '@playwright/test'
import { performAdminSignOut } from '../src/lib/admin-session'

test('로그아웃 성공 후 로그인 화면으로 이동한다', async () => {
  const redirects: string[] = []
  await performAdminSignOut(async () => ({ error: null }), (path) => redirects.push(path))
  expect(redirects).toEqual(['/login'])
})

test('로그아웃 실패 시 이동하지 않고 오류를 전달한다', async () => {
  const redirects: string[] = []
  await expect(performAdminSignOut(
    async () => ({ error: new Error('network') }),
    (path) => redirects.push(path)
  )).rejects.toThrow('network')
  expect(redirects).toEqual([])
})
```

- [ ] **Step 2: 테스트 RED 확인**

Run `npx playwright test tests/admin-session.spec.ts --project=chromium`.

Expected result is FAIL because `src/lib/admin-session.ts` does not exist.

- [ ] **Step 3: 로그아웃 함수 최소 구현**

```ts
// 관리자 세션의 로그아웃 성공과 화면 이동 순서를 보장한다
export async function performAdminSignOut(
  signOut: () => Promise<{ error: Error | null }>,
  redirect: (path: '/login') => void
): Promise<void> {
  const { error } = await signOut()
  if (error) throw error
  redirect('/login')
}
```

- [ ] **Step 4: 로그아웃 테스트 GREEN 확인**

Run `npx playwright test tests/admin-session.spec.ts --project=chromium`.

Expected result is `2 passed`.

- [ ] **Step 5: 공통 레이아웃을 Midnight Command로 교체**

`layout.tsx`에서 기존 인증 `useEffect`는 그대로 유지하고 다음 상태와 핸들러를 추가한다.

```tsx
const [signingOut, setSigningOut] = useState(false)
const [logoutError, setLogoutError] = useState<string | null>(null)

const handleSignOut = async () => {
  setSigningOut(true)
  setLogoutError(null)
  try {
    await performAdminSignOut(
      () => supabase.auth.signOut(),
      (path) => router.replace(path)
    )
  } catch {
    setLogoutError('로그아웃하지 못했습니다. 잠시 후 다시 시도해 주세요.')
  } finally {
    setSigningOut(false)
  }
}
```

레이아웃 JSX는 다음 구조와 책임을 정확히 따른다.

- 루트는 `min-h-screen bg-slate-100 text-slate-950`.
- `md` 이상에서 `fixed inset-y-0 left-0 hidden w-60 flex-col bg-[#0b1730] md:flex` 사이드바를 표시한다.
- 브랜드 마크는 블루·시안 그라디언트, 메뉴 활성 상태는 `bg-blue-600 text-white shadow-lg shadow-blue-950/20`을 사용한다.
- `md` 미만에서는 `sticky top-0 z-40 bg-[#0b1730]` 헤더와 두 탭을 표시한다.
- 콘텐츠 영역은 `md:pl-60`, 내부 최대 폭은 `max-w-[1600px]`로 한다.
- 데스크톱 콘텐츠 상단 오른쪽과 모바일 브랜드 바 오른쪽에 `LogOut` 아이콘을 포함한 버튼을 배치한다.
- 버튼은 `disabled={signingOut}`, 텍스트는 `signingOut ? '로그아웃 중' : '로그아웃'`, 핸들러는 `void handleSignOut()`을 사용한다.
- `logoutError`는 `<div role="alert">`인 레드 경고 배너로 콘텐츠 상단에 표시한다.
- 기존 `/api/admin/me`의 `isAdmin`·`otpVerified` 검사와 `/login`·`/` 리다이렉트는 변경하지 않는다.
- 권한 확인 화면에는 네이비 브랜드 마크와 회전 로더를 표시한다.

- [ ] **Step 6: 타입·린트 검사와 커밋**

Run `npx playwright test tests/admin-session.spec.ts --project=chromium`, `npx tsc --noEmit`, and `npm run lint`.

Expected results are `2 passed` and exit code 0 for both static checks.

```bash
git add src/lib/admin-session.ts src/app/admin/layout.tsx tests/admin-session.spec.ts
git commit -m "feat: 관리자 콘솔 셸과 로그아웃 추가"
```

---

### Task 4: 가입자 관리 정렬·반응형 화면

**Files:**

- Modify `safesys-app/src/app/admin/users/page.tsx:1-622`.

**Interfaces:**

- Consumes Task 1의 `SortState`, `nextSortState`, `sortRows`, `AdminUserSortKey`, `ADMIN_USER_SORT_OPTIONS`, `getAdminUserSortValue`.
- Consumes Task 2의 `SortableHeader`, `MobileSortControls`, `AdminMobileDisclosure`.
- Keeps `adminRequest`, `EditUserModal`, `handleConfirm`, `handleSave`, `handleDelete` behavior unchanged.

- [ ] **Step 1: 가입자 데이터 흐름에 정렬과 펼침 상태 연결**

다음 상태를 `AdminUsersPage`에 추가한다.

```tsx
const [sortState, setSortState] = useState<SortState<AdminUserSortKey>>({ key: null, direction: null })
const [expandedUserId, setExpandedUserId] = useState<string | null>(null)
```

필터 이후와 페이지 분할 이전에 다음 파생 데이터를 둔다.

```tsx
const sortedUsers = useMemo(
  () => sortRows(filteredUsers, sortState, getAdminUserSortValue),
  [filteredUsers, sortState]
)
const totalPages = Math.max(1, Math.ceil(sortedUsers.length / PAGE_SIZE))
const currentPage = Math.min(page, totalPages)
const visibleUsers = sortedUsers.slice((currentPage - 1) * PAGE_SIZE, currentPage * PAGE_SIZE)
```

정렬 변경 함수와 상태 초기화 규칙을 추가한다.

```tsx
const resetListPosition = () => {
  setPage(1)
  setExpandedUserId(null)
}

const handleSort = (key: AdminUserSortKey) => {
  setSortState((current) => nextSortState(current, key))
  resetListPosition()
}

const handleMobileSortChange = (state: SortState<AdminUserSortKey>) => {
  setSortState(state)
  resetListPosition()
}
```

검색, 역할 필터, 미인증 필터, 이전·다음 페이지, `loadUsers` 완료 시 `expandedUserId`를 `null`로 만든다. 검색·필터·정렬은 페이지를 1로 돌리고 명시적 페이지 이동은 목적 페이지를 유지한다.

- [ ] **Step 2: Midnight Command 페이지 헤더·통계·필터 적용**

- 페이지 eyebrow는 `MEMBER DIRECTORY`, 제목은 `가입자 관리`, 설명은 기존 문구를 유지한다.
- 새로고침 버튼은 44px 높이, 블루 포커스 링, 진행 중 회전 아이콘을 사용한다.
- `SummaryCards`는 전체·발주청·감리단·시공사·미인증 5개를 `md:grid-cols-5`로 표시한다.
- 각 카드에는 `UsersRound`, `Landmark`, `ClipboardCheck`, `HardHat`, `Clock3` 아이콘과 상태별 블루·민트·앰버 표면을 적용한다.
- 모바일 통계 영역은 `flex overflow-x-auto`, 각 카드는 `min-w-[132px]`로 한다.
- 검색·역할·미인증 필터는 흰색 라운드 패널 안에 배치하고 모바일에서 세로, `md` 이상에서 가로로 배치한다.
- 필터 패널 다음에 `<MobileSortControls options={ADMIN_USER_SORT_OPTIONS} sortState={sortState} onChange={handleMobileSortChange} />`를 배치한다.

- [ ] **Step 3: 데스크톱 정렬 테이블 구현**

기존 테이블 컨테이너에 `hidden md:block`을 적용하고 작업 열을 제외한 9개 헤더를 모두 다음 패턴으로 교체한다.

```tsx
<SortableHeader label="이름" sortKey="name" sortState={sortState} onSort={handleSort} />
<SortableHeader label="이메일" sortKey="email" sortState={sortState} onSort={handleSort} />
<SortableHeader label="역할" sortKey="role" sortState={sortState} onSort={handleSort} />
<SortableHeader label="본부·지사" sortKey="organization" sortState={sortState} onSort={handleSort} />
<SortableHeader label="회사" sortKey="company" sortState={sortState} onSort={handleSort} />
<SortableHeader label="직책" sortKey="position" sortState={sortState} onSort={handleSort} />
<SortableHeader label="가입일" sortKey="createdAt" sortState={sortState} onSort={handleSort} />
<SortableHeader label="최근 로그인" sortKey="lastSignInAt" sortState={sortState} onSort={handleSort} />
<SortableHeader label="인증 상태" sortKey="confirmed" sortState={sortState} onSort={handleSort} />
<th scope="col" className="px-3 py-2 text-right font-semibold text-slate-600">작업</th>
```

본문 데이터와 기존 인증·수정·삭제 버튼 동작은 유지하되 행 높이, hover, 배지, 포커스 링을 Midnight Command 토큰으로 통일한다. 검색 결과와 페이지 수는 `sortedUsers.length`를 사용한다.

- [ ] **Step 4: 모바일 접힌 가입자 목록 구현**

테이블 앞에 `md:hidden` 목록을 만들고 각 `visibleUsers`를 다음 구조로 렌더링한다.

```tsx
<AdminMobileDisclosure
  key={user.id}
  id={user.id}
  expanded={expandedUserId === user.id}
  onToggle={() => setExpandedUserId((current) => current === user.id ? null : user.id)}
  summary={(
    <div className="flex items-center gap-3">
      <span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-blue-50 font-bold text-blue-700">
        {(user.full_name ?? user.email ?? '?').slice(0, 1)}
      </span>
      <span className="min-w-0 flex-1">
        <span className="block truncate font-semibold text-slate-950">{user.full_name ?? '이름 없음'} · {user.role ?? '역할 없음'}</span>
        <span className="mt-0.5 block truncate text-xs text-slate-500">{user.email ?? '이메일 없음'}</span>
      </span>
      <span className={user.email_confirmed_at ? 'rounded-full bg-emerald-50 px-2 py-1 text-xs font-semibold text-emerald-700' : 'rounded-full bg-amber-50 px-2 py-1 text-xs font-semibold text-amber-700'}>
        {user.email_confirmed_at ? '인증' : '대기'}
      </span>
    </div>
  )}
>
  <dl className="grid grid-cols-2 gap-x-4 gap-y-3 text-sm">
    <div><dt className="text-xs text-slate-400">소속</dt><dd className="mt-1 font-medium text-slate-700">{[user.hq_division, user.branch_division].filter(Boolean).join(' · ') || '-'}</dd></div>
    <div><dt className="text-xs text-slate-400">회사</dt><dd className="mt-1 font-medium text-slate-700">{user.company_name ?? '-'}</dd></div>
    <div><dt className="text-xs text-slate-400">직책</dt><dd className="mt-1 font-medium text-slate-700">{user.position ?? '-'}</dd></div>
    <div><dt className="text-xs text-slate-400">가입일</dt><dd className="mt-1 font-medium text-slate-700">{formatDate(user.created_at)}</dd></div>
    <div className="col-span-2"><dt className="text-xs text-slate-400">최근 로그인</dt><dd className="mt-1 font-medium text-slate-700">{formatDate(user.last_sign_in_at, true)}</dd></div>
  </dl>
  <div className="mt-4 grid grid-cols-2 gap-2">
    {!user.email_confirmed_at && <button type="button" onClick={() => void handleConfirm(user)} disabled={Boolean(pendingAction)} className="min-h-11 rounded-xl bg-emerald-600 px-3 text-sm font-semibold text-white disabled:opacity-50">인증 처리</button>}
    <button type="button" onClick={() => setEditingUser(user)} disabled={Boolean(pendingAction)} className="min-h-11 rounded-xl bg-[#15396f] px-3 text-sm font-semibold text-white disabled:opacity-50">정보 수정</button>
    <button type="button" onClick={() => void handleDelete(user)} disabled={Boolean(pendingAction)} className="min-h-11 rounded-xl border border-red-200 bg-red-50 px-3 text-sm font-semibold text-red-700 disabled:opacity-50">계정 삭제</button>
  </div>
</AdminMobileDisclosure>
```

상세 작업 버튼은 disclosure의 토글 버튼 밖에 있으므로 중첩 버튼과 토글 이벤트 중복이 없어야 한다.

- [ ] **Step 5: 가입자 화면 정적 검증과 커밋**

Run `npx playwright test tests/admin-sort.spec.ts tests/admin-controls.spec.tsx --project=chromium`, `npx tsc --noEmit`, and `npm run lint`.

Expected results are `10 passed` and exit code 0 for both static checks.

```bash
git add src/app/admin/users/page.tsx
git commit -m "feat: 가입자 관리 화면 반응형 개편"
```

---

### Task 5: 프로젝트 현황 정렬·반응형 화면

**Files:**

- Modify `safesys-app/src/app/admin/projects/page.tsx:1-412`.

**Interfaces:**

- Consumes Task 1의 `SortState`, `nextSortState`, `sortRows`, `AdminProjectSortKey`, `ADMIN_PROJECT_SORT_OPTIONS`, `getAdminProjectSortValue`.
- Consumes Task 2의 `SortableHeader`, `MobileSortControls`, `AdminMobileDisclosure`.
- Keeps `loadProjects` and `handleDelete` API behavior unchanged.

- [ ] **Step 1: 프로젝트 데이터 흐름에 정렬과 펼침 상태 연결**

```tsx
const [sortState, setSortState] = useState<SortState<AdminProjectSortKey>>({ key: null, direction: null })
const [expandedProjectId, setExpandedProjectId] = useState<string | null>(null)

const sortedProjects = useMemo(
  () => sortRows(filteredProjects, sortState, getAdminProjectSortValue),
  [filteredProjects, sortState]
)
const totalPages = Math.max(1, Math.ceil(sortedProjects.length / PAGE_SIZE))
const visiblePage = Math.min(currentPage, totalPages)
const pageProjects = sortedProjects.slice((visiblePage - 1) * PAGE_SIZE, visiblePage * PAGE_SIZE)
```

다음 정렬 핸들러로 현재 페이지를 1로 돌리고 펼친 프로젝트를 닫는다.

```tsx
const resetListPosition = () => {
  setCurrentPage(1)
  setExpandedProjectId(null)
}

const handleSort = (key: AdminProjectSortKey) => {
  setSortState((current) => nextSortState(current, key))
  resetListPosition()
}

const handleMobileSortChange = (state: SortState<AdminProjectSortKey>) => {
  setSortState(state)
  resetListPosition()
}
```

본부·지사·검색·활성 필터와 새로고침도 `setExpandedProjectId(null)`을 실행한다. 명시적 페이지 이동 핸들러는 목적 페이지를 `setCurrentPage`로 적용한 뒤 펼친 항목만 닫는다.

- [ ] **Step 2: Midnight Command 프로젝트 헤더·통계·필터 적용**

- eyebrow는 `PROJECT CONTROL`, 제목은 `프로젝트 등록 현황`으로 한다.
- 전체·활성·비활성·최근 30일 등록 카드에 `FolderKanban`, `CircleCheck`, `CirclePause`, `CalendarPlus` 아이콘을 사용한다.
- 본부별 등록 현황은 가로 스크롤 가능한 pill 그룹으로 만들고 선택 상태는 블루 배경으로 표시한다.
- 본부·지사·검색·활성 필터는 가입자 화면과 같은 표면·44px 높이·포커스 링을 사용한다.
- 필터 패널 다음에 `<MobileSortControls options={ADMIN_PROJECT_SORT_OPTIONS} sortState={sortState} onChange={handleMobileSortChange} />`를 배치한다.

- [ ] **Step 3: 데스크톱 프로젝트 정렬 테이블 구현**

기존 테이블에 `hidden md:block`을 적용하고 헤더를 다음과 같이 교체한다.

```tsx
<SortableHeader label="프로젝트명" sortKey="name" sortState={sortState} onSort={handleSort} />
<SortableHeader label="본부" sortKey="hq" sortState={sortState} onSort={handleSort} />
<SortableHeader label="지사" sortKey="branch" sortState={sortState} onSort={handleSort} />
<SortableHeader label="등록자" sortKey="creator" sortState={sortState} onSort={handleSort} />
<SortableHeader label="등록일" sortKey="createdAt" sortState={sortState} onSort={handleSort} />
<SortableHeader label="상태" sortKey="status" sortState={sortState} onSort={handleSort} />
<th scope="col" className="px-4 py-2 text-right font-semibold text-slate-600">관리</th>
```

프로젝트 링크, 등록자 보조 회사명, 상태 배지, 삭제 버튼 동작은 유지한다. 검색 결과, 표시 범위, 페이지 수 계산은 `sortedProjects.length`를 사용한다.

- [ ] **Step 4: 모바일 접힌 프로젝트 목록 구현**

```tsx
<AdminMobileDisclosure
  key={project.id}
  id={project.id}
  expanded={expandedProjectId === project.id}
  onToggle={() => setExpandedProjectId((current) => current === project.id ? null : project.id)}
  summary={(
    <div className="flex min-w-0 items-center gap-3">
      <span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-blue-50 text-blue-700"><FolderKanban className="h-5 w-5" /></span>
      <span className="min-w-0 flex-1"><span className="block truncate font-semibold text-slate-950">{project.project_name}</span><span className="mt-0.5 block truncate text-xs text-slate-500">{project.managing_hq} · {project.managing_branch}</span></span>
      <span className={project.isActive ? 'rounded-full bg-emerald-50 px-2 py-1 text-xs font-semibold text-emerald-700' : 'rounded-full bg-slate-100 px-2 py-1 text-xs font-semibold text-slate-600'}>{project.isActive ? '활성' : '비활성'}</span>
    </div>
  )}
>
  <dl className="grid grid-cols-2 gap-x-4 gap-y-3 text-sm">
    <div><dt className="text-xs text-slate-400">등록자</dt><dd className="mt-1 font-medium text-slate-700">{project.creator?.fullName || '등록자 정보 없음'}</dd></div>
    <div><dt className="text-xs text-slate-400">회사</dt><dd className="mt-1 font-medium text-slate-700">{project.creator?.companyName || '-'}</dd></div>
    <div className="col-span-2"><dt className="text-xs text-slate-400">등록일</dt><dd className="mt-1 font-medium text-slate-700">{formatDate(project.created_at)}</dd></div>
  </dl>
  <div className="mt-4 grid grid-cols-2 gap-2">
    <Link href={`/project/${project.id}`} target="_blank" rel="noreferrer" className="inline-flex min-h-11 items-center justify-center rounded-xl bg-[#15396f] px-3 text-sm font-semibold text-white">프로젝트 열기</Link>
    <button type="button" onClick={() => void handleDelete(project)} disabled={deletingId !== null} className="min-h-11 rounded-xl border border-red-200 bg-red-50 px-3 text-sm font-semibold text-red-700 disabled:opacity-50">{deletingId === project.id ? '삭제 중' : '삭제'}</button>
  </div>
</AdminMobileDisclosure>
```

- [ ] **Step 5: 프로젝트 화면 정적 검증과 커밋**

Run `npx playwright test tests/admin-sort.spec.ts tests/admin-controls.spec.tsx --project=chromium`, `npx tsc --noEmit`, and `npm run lint`.

Expected results are `10 passed` and exit code 0 for both static checks.

```bash
git add src/app/admin/projects/page.tsx
git commit -m "feat: 프로젝트 관리 화면 반응형 개편"
```

---

### Task 6: 전체 회귀 검증과 반응형 확인

**Files:**

- Modify only files from Tasks 1-5 if verification exposes a defect.

**Interfaces:**

- Verifies the complete `/admin/users` and `/admin/projects` flow without changing API contracts.

- [ ] **Step 1: 전체 관리자 자동 테스트 실행**

Run `npx playwright test tests/admin-sort.spec.ts tests/admin-controls.spec.tsx tests/admin-session.spec.ts --project=chromium`.

Expected result is `12 passed` with zero failures.

- [ ] **Step 2: 전체 정적 검사 실행**

Run `npm run lint` and expect exit code 0.

Run `npx tsc --noEmit` and expect exit code 0.

Run `git diff --check` and expect no output.

- [ ] **Step 3: 데스크톱 브라우저 검증**

관리자 OTP 로그인 후 1440px 너비에서 다음을 확인한다.

- `/admin/users`와 `/admin/projects`의 네이비 셸, 활성 메뉴, 통계 카드, 필터, 표가 깨지지 않는다.
- 작업 열을 제외한 모든 헤더가 없음·오름차순·내림차순·없음으로 순환한다.
- 필터링된 전체 결과가 정렬된 뒤 50개 단위로 페이지 분할된다.
- 인증·수정·삭제·프로젝트 열기 작업이 기존 API를 호출한다.
- 우측 상단 로그아웃 버튼이 표시되고 성공 시 `/login`으로 이동한다.

- [ ] **Step 4: 390px 모바일 브라우저 검증**

다시 관리자 OTP 로그인 후 390px 너비에서 다음을 확인한다.

- 페이지 자체에 가로 스크롤이 없다.
- 가입자·프로젝트는 접힌 목록으로 시작한다.
- 한 항목을 누르면 상세가 펼쳐지고, 다른 항목을 누르면 기존 항목이 닫힌다.
- 같은 항목을 다시 누르면 닫힌다.
- 검색·필터·정렬·페이지 이동·새로고침 시 펼친 항목이 닫힌다.
- 모바일 정렬 기준과 방향 결과가 데스크톱과 동일하다.
- 모든 주요 터치 컨트롤의 높이가 44px 이상이다.
- 우측 상단 로그아웃 버튼을 한 손으로 조작할 수 있다.

- [ ] **Step 5: 검증 중 발견한 결함을 TDD로 수정**

결함마다 먼저 해당 증상을 재현하는 실패 테스트를 기존 `admin-*.spec.ts(x)`에 추가하고 RED를 확인한다. 최소 수정 후 대상 테스트, 전체 관리자 테스트, 린트, 타입 검사를 다시 실행한다.

- [ ] **Step 6: 최종 검증 커밋**

검증 수정이 있었다면 관련 파일과 테스트만 선별해 커밋한다.

```bash
git add src/app/admin src/components/admin src/lib/admin-sort.ts src/lib/admin-list-sort.ts src/lib/admin-session.ts tests/admin-sort.spec.ts tests/admin-controls.spec.tsx tests/admin-session.spec.ts
git commit -m "fix: 관리자 콘솔 반응형 검증 보완"
```

수정이 없으면 빈 커밋을 만들지 않는다. 프로덕션 빌드는 실행하지 않는다.
