# 컨텍스트 노트.

- 요청 원문. "(AI) 순회점검대장 하나 넣어주고, 입력 형태는 공사감독_순회점검.hwpx 이 형태로, TBM 작업내용에 맞춰 점검항목이 결정, ai는 gpt 5.6 luna, 출력시에도 한글 양식대로."
- 클릭한 요소는 발주청 캐비닛의 C(점검) 그룹(`src/app/project/[id]/page.tsx` 1757행 부근, `︵AI︶ 공사감독 일지` 옆)이다. 안전 캐비닛의 C(점검) 그룹이 아니다.
- 표 1 열 그리드 = [3406, 26198, 3765, 8273, 9076] (합 50718). r3~r12의 점검사항 셀은 c1 colSpan 3, 결과 셀은 c4. r13은 c0 사진 라벨(세로 글자 4런), c1 사진, c2 지적사항 라벨, c3 colSpan 2 내용. 행 높이 제목 2751, 빈 줄 1045, 머리글 2825, 항목 3092, 사진행 18750(최소값).
- 표 2 열 그리드 = [4831, 4396, 12604, 3825, 6656, 4108, 8139, 6441] (합 51000), 행 높이 3589.
- 양식의 `(작업장 공통)` 텍스트는 셀 안에서 hp:t 런이 둘로 나뉜 곳이 있다. 셀 본문 전체를 새 문단으로 갈아 끼우는 방식이 안전하다.
- 한글 실행파일은 `C:\Program Files (x86)\Hnc\Office 2022\HOffice120\Bin\Hwp.exe` (hwpx-authoring 스킬의 2020 경로는 낡았다). `scripts/render-pdf.ps1`은 없다 — COM으로 PDF 저장 후 Read 도구로 PDF를 직접 본다.
- `ai.patrol-inspection`은 관리자 AI 인벤토리(`DEFAULT_AI_MODELS`)에 넣지 않았다. 모델 고정 기능은 같은 선례를 따른다.
- 사진 버킷 `safety-inspection-photos`는 공개 URL(getPublicUrl)을 쓴다.
