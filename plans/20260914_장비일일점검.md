# AI 장비 일일점검 대장 구현 계획

**목표.** 시공 C(점검)에 대장을 추가하고 PDF 장비별 점검, 서명 제출, 제출 목록 HWPX 다운로드를 구현한다.
**구조.** 기존 Next.js 프로젝트 권한과 Supabase 패턴을 재사용한다. PDF의 체크 내용을 정적 카탈로그로 보존하며 날짜별 점검 스냅샷을 저장한다. AI라는 대장 명칭은 사용자 지정이며 점검 항목을 AI로 임의 생성하지 않는다.
**기술.** Next.js, React, Supabase, JSZip, 기존 HWPX TBM 헬퍼.
**원본.** `docs/일일안전점검 체크리스트 양식.pdf` (29쪽).

## 공통 계약

Codex worker 소유 `safesys-app/src/lib/equipment-inspection-types.ts`.
```ts
export type EquipmentInspectionResult = 'pass' | 'fail' | 'na'
export interface EquipmentChecklistItem { id: string; category: string; text: string }
export interface EquipmentChecklist { id: string; name: string; sourcePage: number; items: EquipmentChecklistItem[] }
export interface EquipmentInspectionAnswer extends EquipmentChecklistItem { result: EquipmentInspectionResult; note: string }
export interface EquipmentInspection {
  id: string; project_id: string; equipment_type: string; equipment_name: string;
  inspection_date: string; company_name: string; vehicle_number: string; machine_number: string;
  inspector_name: string; signature: string; answers: EquipmentInspectionAnswer[];
  remarks: string; created_by: string | null; created_at: string;
}
```
카탈로그 `src/lib/equipment-inspection-catalog.ts`는 `EQUIPMENT_CHECKLISTS: EquipmentChecklist[]`를 export한다.
HWPX `src/lib/hwpx/equipment-inspection-hwpx-export.ts`는 `downloadEquipmentInspectionHwpx(record: EquipmentInspection, projectName: string): Promise<void>`와 하니스에서 쓸 blob 빌더를 export한다.

## Task 1. Claude Opus — 화면·데이터·SQL

- 소유. `src/app/project/[id]/equipment-inspection/page.tsx`, 새 폼/목록 컴포넌트, `src/lib/equipment-inspections.ts`, 프로젝트 `page.tsx` 서류철 연결, 신규 SQL, merge SQL fixture/test, 관련 문서.
- 점검하기 → 23종 장비 선택 → 기본사항·항목별 적합/부적합/해당없음·비고 → 점검자 직접 서명 → 제출 → 목록/상세/HWPX 다운로드.
- 미점검·빈 서명 제출 차단, 장비 교체 시 이전 응답과 서명 초기화, 조회 오류/빈 목록 구분, 저장 중 중복 클릭 방지.
- 인증·프로젝트 관할 RLS, created_by 위조 방지, projects FK CASCADE 및 merge_projects_safe_v2 새 테이블 이동·개수 가드·회귀 테스트 보강.
- SQL만 제공하고 실제 DB에 적용하지 않는다. 개인 점검자 서명은 일괄서명에서 제외한다.
- [ ] 유효성·RLS/병합 테스트 RED 확인.
- [ ] 구현 후 테스트 GREEN, 타입·린트 확인.

## Task 2. Codex gpt-6-astra low — 원본 카탈로그·HWPX

- 소유. 위 공통 타입·카탈로그·HWPX 모듈 및 해당 테스트/검증 하니스.
- PDF 전체 장비 원문을 추출하고 23종 및 각 항목 수·누락 여부 검증. 원본 수치는 현행 법규로 주장하지 않고 출처 양식 그대로 취급한다.
- TBM 코드 조립의 필수 이미지 XML·패키지 구조를 재사용하고 서명은 안내문구 위에 겹치되 이름을 가리지 않는다.
- 긴 문구/비고는 페이지를 명시적으로 분할하여 잘림을 방지한다.
- [ ] 카탈로그·서명 이미지·장문 페이지 분할 테스트 RED 확인.
- [ ] 구현 및 테스트 GREEN, 실제 서명 포함 HWPX 샘플 생성.

## Task 3. Advisor 통합 검증

- [ ] diff 직접 리뷰 후 필요 시 동일 worker에 수정 위임.
- [ ] 타입체크·린트·기능 테스트·병합 SQL 회귀 검사.
- [ ] Orca 브라우저 1275×922 및 모바일 점검 흐름 검증. DB 미적용은 명시하고 실제 제출은 실행하지 않는다.
- [ ] 한글 2020 열기 ALIVE, COM PDF 변환, 모든 출력 페이지 렌더 육안 검사.
- [ ] 계획/체크리스트 갱신 및 해당 변경만 커밋. 원본 사용자 PDF와 기존 log.md 변경은 커밋에 포함하지 않는다. build/push/DB 적용 없음.

## 재사용 조사

GitHub `gh search repos hwpx`로 rhwp/hwpxskill/hwpxlib 확인. 본 리포 TBM exporter가 한글2020 서명 호환성까지 해결하므로 우선 재사용한다. JSZip 공식 `generateAsync` 문서의 blob/DEFLATE/MIME 사용법 확인. 새 라이브러리는 추가하지 않는다.
