# 재해예방 계약현황 카드 — 컨텍스트 노트 (2026-07-08)

작업 중 내린 결정과 근거. 계획서: `20260708_재해예방_계약현황_카드.md`.

## 데이터 실태 (2026-07-08 service role 읽기 조회)

- `project_contracts` 총 287행 (공사 107, 용역 180). 계약 보유 프로젝트 47개 — 전부 경기본부 소속 지사.
- **재해예방기술지도 용역이 실제로 등록되어 있음.** 계약명 패턴: `<공사 계약명> + " 재해예방기술지도용역"` (붙임/띄움 혼재). 예: 모전지구 공사 "모전지구 농업용수관로정비사업 토목공사" ↔ 용역 "모전지구 농업용수관로정비사업 토목공사 재해예방기술지도용역" (corp_nm=지도기관).
- `projects.managing_hq`는 "경기"처럼 **"본부" 접미어 없이** 저장됨 → 엑셀 본부명은 `본부` 접미어 부착(본사·기타 제외). 지사명은 "여주·이천지사"처럼 가운뎃점 포함 — 그대로 사용.
- projects 전체 958행 (충남 234, 경기 174, 경북 165, …).

## 설계 결정

1. **화면도 엑셀과 동일한 '재해예방' 관점** — 전체 계약이 아니라 공사 계약 + 매칭되는 재해예방기술지도 용역만 표시. 카드명이 "재해예방 계약현황"이므로 감리 등 기타 용역은 제외.
2. **매칭 규칙**: 공백 제거+차수 접미어 제거 정규화 후, 용역명이 공사명으로 시작하면 매칭. 폴백: 프로젝트에 공사 1건·지도용역 1건뿐이면 매칭. 미매칭 지도용역도 행으로 노출(공사명은 "재해예방기술지도 용역" 접미어 제거로 유도, 공사 금액·업체는 공란).
3. **차수 병합**: contract-status 페이지의 `nameGroupKey`와 동일 로직(타입|공백제거·차수제거 계약명). 대표 행=최신 cntrct_date, 기간=min(start)~max(end), 금액=대표 tot_cntrct_amt.
4. **신규 라우트** `/disaster-prevention-contracts` (발주청 전용 standalone 페이지). Dashboard(4,900줄)에 뷰를 넣지 않고 분리 — 파일 비대화 방지.
5. **조회 쿼리**: `project_contracts` + `projects!inner(...)` 조인 1회 조회, `getProjectsByUserBranch`와 동일한 범위 규칙(본사=전사, 본부 대표 지사=본부 전체, 일반 지사=해당 지사)을 조인 컬럼 필터로 재현. 287행 규모라 청크 불필요.
6. **엑셀 양식 재현**: A열 여백(1.7), B~R 데이터, B2:K3 제목(HY헤드라인M 16pt), 5~7행 3단 헤더(Dotum 9pt bold 흰 글씨·검정 배경·medium 테두리), 데이터 행 Dotum 9pt·thin 테두리·가운데, 금액 회계서식 `_-* #,##0_-;-* #,##0_-;_-* "-"_-;_-@_-`, 행높이 헤더 18·데이터 26.25. 공사기간 YYYY.MM.DD, 지도 계약기간 YY.MM.DD (예시 파일 관례).
7. **못 채우는 컬럼**(지방고용노동청·사업장구분·지도기관 소재지·기술지도 구분·비고)은 공란 — DB에 없는 데이터고, 예시 파일에서도 기술지도 구분 값이 비일관(전기공사를 건설공사로 적은 행 존재)이라 추정 기입은 오히려 위험.
8. **소재지 추출**: site_address 토큰 중 광역시·특별시류를 제외한 첫 `…시|군` 토큰 (예: "경기도 이천시 백사면…" → "이천시"). 실패 시 첫 토큰.
9. **카드 위치**: 최초 /list 검색바 위 배너형으로 구현했으나 사용자 피드백(2026-07-08)으로 **/business 사업현황 카드 그리드의 "재해예방기술지도" 카드**로 이동(기존 4개 카드와 동일한 그리드 타일, 클릭 → 신규 페이지, 뒤로가기도 /business). 건수 표기는 대시보드에서 추가 쿼리가 필요해 "계약 현황" 고정 문구로 대체.
10. DB 조회용 MCP가 세션에 없어 service role 키(.env.local)로 읽기 전용 Node 스크립트 조회를 사용했다.
