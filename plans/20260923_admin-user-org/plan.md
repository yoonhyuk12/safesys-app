<!-- 관리자 가입자 소속 선택 변경 계획 -->
# 관리자 가입자 소속 선택

기존 본부·지사 입력을 공용 상수를 사용하는 select로 교체한다. 현재 FIELD_CLASS와 라벨, 역할 노출 및 저장 형식을 유지한다.

초기 레거시 값은 임시 현재 값 옵션으로 보존하고, 본부 변경 이벤트에서만 새 본부와 호환되지 않는 지사를 비운다. 기존 Node 테스트 러너와 TypeScript 변환으로 실제 모달의 옵션·변경 이벤트·저장 동작을 검증한다. 외부 라이브러리나 새 의존성이 필요 없는 기존 패턴 재사용이다.
