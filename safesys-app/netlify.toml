# Netlify 배포 설정 — safesys-app (Next.js 15). Vercel과 병행하는 운영 접속 주소 추가용.
[build]
  command = "next build"
  publish = ".next"

[build.environment]
  # React 19 peer 의존성 때문에 Vercel과 동일하게 legacy-peer-deps 사용.
  NPM_FLAGS = "--legacy-peer-deps"

[[plugins]]
  package = "@netlify/plugin-nextjs"
